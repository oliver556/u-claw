# U-Claw 第四阶段真实后端接线设计

> 记录日期：2026-08-11  
> 目标：把第一、二、三阶段已完成的 UI、契约、Store 和本地服务接到真实 OpenClaw 与已有后端，清零生产运行时的 Mock 与固定空数据。

## 1. 范围与完成定义

第四阶段只做真实能力接线和联调收口，不重写 OpenClaw Agent 引擎，不重做已有 React UI，不扩展新产品需求。

标准调用链：

```text
用户真实操作
-> React UI
-> preload 白名单 API
-> Electron 受控 IPC
-> U-Claw Adapter / 领域服务
-> OpenClaw Gateway、CLI、配置或 U 盘数据目录
-> 真实结果返回 UI
```

每个功能只有同时满足以下条件才能标记完成：

1. 生产 Electron 使用真实 wiring，不使用 `MockUClawClient`、fixture 或只供测试的 executor。
2. UI 读取的是真实权威数据，写操作可被 OpenClaw 或对应后端读回。
3. 成功、空数据、无服务、凭据错误、网络错误和不支持状态均可区分。
4. 至少有一条非 Mock 集成证据；外部账号或 Windows 限定项允许在本阶段标记“待真实环境验收”，但不得标记“已接通”。
5. 浏览器预览明确标识为开发模式；不用静态假数据冒充真实状态。

## 2. Task 拆分

| Task | 范围 | 完成门槛 |
| --- | --- | --- |
| `P4-T01` | Electron 与 OpenClaw 生产装配 | 正式组装 Gateway transport、`OpenClawClient`、领域服务、IPC dispatcher 和生产 executor；未配置时可诊断失败 |
| `P4-T02` | 会话、上下文、文件与记忆 | 刷新后会话不丢；固定/分组落盘；文件和记忆与 OpenClaw/U 盘读回一致 |
| `P4-T03` | Provider、模型与三类路由 | 配置、Key、发现、连通测试和实际对话使用同一有效配置；外部错误不静默回退 |
| `P4-T04` | Skill、Plugin 与 MCP | 列表、安装/更新/卸载、启停和 OpenClaw 实际可用状态一致 |
| `P4-T05` | Telegram、QQ、飞书、企微与个人微信 | 每个渠道配置进入真实 runtime；统一状态来自后端而非 UI Store |
| `P4-T06` | 活动、成果、日志、诊断、维护与更新 | 页面使用真实任务/工具/渠道/更新/错误事件和真实系统服务 |
| `P4-T07` | 全 UI 逐项联调会合 | 矩阵每项均有权威源、调用链、成功/失败证据；非明确开发模式不再使用 Mock |

`P4-T01` 是其他 Task 的前置。`P4-T02`–`P4-T06` 可在装配契约稳定后并行。`P4-T07` 只做会合与缺口清零，不用 Mock 替代失败项。

## 3. UI 与已有后端接线矩阵

| UI 域 | 需联调的用户功能 | 真实权威源 | Task |
| --- | --- | --- | --- |
| 工作/会话 | 会话列表、新建、切换、重命名、删除、历史分页、刷新恢复 | OpenClaw Gateway `sessions.*` / `chat.history` | `P4-T02` |
| 工作/对话 | 发送、流式输出、停止、断线恢复、Markdown 安全渲染 | OpenClaw Gateway `chat.*` | `P4-T02` |
| 工作/附件 | 选择、拖放、发送、进度、失败重试 | OpenClaw 附件/消息协议 + U 盘工作区 | `P4-T02` |
| 工作/工具 | 工具调用状态、参数/结果摘要、授权、失败原因 | OpenClaw tools / approvals 事件 | `P4-T02` |
| 工作/整理 | 搜索、固定、分组 | OpenClaw 会话 + U 盘 `session-organizer.json` | `P4-T02` |
| 工作/模型 | 当前会话模型选择、写后读回、失效提示 | OpenClaw `sessions.patch` / models | `P4-T03` |
| 上下文 | 附件、引用文件、记忆、工具结果、任务成果 | OpenClaw 会话/消息/工具 + U 盘文件 | `P4-T02` |
| 活动 | 长任务、工具、审批、渠道、更新和错误聚合 | OpenClaw 事件 + Desktop 领域事件 | `P4-T06` |
| 成果 | 成果列表、打开、来源会话回溯 | OpenClaw 消息 artifact + U 盘工作区 | `P4-T06` |
| 文件 | 列表、搜索、打开、显示位置、重命名、移动、删除 | `.uclaw/data/workspace` + Desktop 受控文件服务 | `P4-T02` |
| 记忆 | `MEMORY.md` 和 `memory/*.md` 列表、搜索、编辑、删除、读回 | OpenClaw 实际记忆目录 + Desktop 文件服务 | `P4-T02` |
| Provider | 新增、编辑、启停、排序、API Key 轮换/脱敏 | Desktop Provider Store + 安全凭据存储 | `P4-T03` |
| Provider | OpenAI-compatible 自定义服务、国产 Provider 实际对话 | 生产 `domestic/custom` executor | `P4-T03` |
| 本地模型 | Ollama/LM Studio 发现、选择、连通测试 | 本机端口/API + Provider network service | `P4-T03` |
| 模型路由 | `builtin/domestic/custom` 选择与实际请求 | Model Source Router + New API/外部 Provider | `P4-T03` |
| 代理 | Proxy 和 `NO_PROXY` 保存、测试、运行时生效 | Desktop network service + Gateway/模型 executor 环境 | `P4-T03` |
| Skill | 已安装列表、版本、来源、更新、启停、OpenClaw 可用状态 | OpenClaw Skill 目录/配置 + U 盘能力目录 | `P4-T04` |
| SkillHub | 榜单、搜索、分类、详情、免费 Skill 安装/更新/卸载 | SkillHub.cn + Desktop Skill Service | `P4-T04` |
| Plugin | 列表、安装、更新、卸载、启停和状态 | OpenClaw CLI/runtime + Plugin Registry | `P4-T04` |
| MCP | 列表、新增、编辑、启停、删除、连接与工具暴露 | OpenClaw `config.get/config.patch` + MCP runtime | `P4-T04` |
| 能力风险 | Skill/Plugin/MCP 权限、风险确认和审计 | Desktop 权限策略 + OpenClaw 实际调用 | `P4-T04` |
| Telegram | 配置、启停、状态、消息收发和断开 | OpenClaw Telegram runtime | `P4-T05` |
| QQ Bot | 配置、启停、状态、消息收发和断开 | OpenClaw QQ Plugin/runtime | `P4-T05` |
| 飞书 | 配置、启停、状态、回调和消息收发 | OpenClaw 飞书 runtime | `P4-T05` |
| 企业微信 | 配置、启停、状态、回调和消息收发 | OpenClaw 企微 runtime | `P4-T05` |
| 个人微信 | 扫码、刷新、超时、登录、失效和退出 | OpenClaw 微信插件 + Windows 微信客户端 | `P4-T05` |
| 连接总览 | 全渠道健康、凭据状态、待操作项 | 各真实渠道 runtime 聚合 | `P4-T05` |
| 数据维护 | 备份预览、恢复、保留、回滚、空间统计、受控清理 | U 盘数据目录 + Gateway 生命周期协调器 | `P4-T06` |
| 系统/日志 | 真实日志、筛选、搜索、导出、清理 | OpenClaw/Desktop/Launcher 日志服务 | `P4-T06` |
| 系统/诊断 | Doctor、受控修复、DNS、代理、端口、认证、模型诊断 | OpenClaw Doctor/CLI + Desktop 诊断 executor | `P4-T06` |
| 系统/信息 | 系统信息、原始配置脱敏读取/导出 | OS/Desktop + OpenClaw `config.get` | `P4-T06` |
| 系统/恢复 | 恢复出厂范围、备份、二次确认和执行 | Desktop maintenance + Gateway 生命周期协调器 | `P4-T06` |
| 系统/更新 | 检查、下载、签名验证、切换、回滚、本机缓存清理 | HTTPS release feed + Desktop/Launcher release service | `P4-T06` |
| 设备状态 | U 盘、数据落盘、Gateway、当前模型常驻状态 | Launcher/Desktop/Gateway/Model Router | `P4-T01`、`P4-T03` |
| 内置服务 | 默认模型对话、额度/限速/模型权限/禁用生效 | New API data plane + 运营控制面 | `P4-T03`、`P4-T07` |
| 授权与制盘 | 启动验签、许可证生命周期、USB 身份、自动开户和失败补偿 | Launcher + License Service + New API management | `P4-T07` |

## 4. 联调环境配置

项目根目录使用两个文件：

- `.env.example`：可提交的完整字段模板，每个字段注明用途、是否必填和安全边界。
- `.env`：本机联调值，已由 `.gitignore` 忽略，禁止提交、截图或写入日志。

字段按下列组织，不把所有值都设为必填：

1. OpenClaw Runtime/Gateway：本地 runtime、Gateway URL/端口、可选鉴权、CLI 路径。
2. 数据与开发：U-Claw 数据目录、renderer URL、明确的 Mock 开关。
3. Provider：国产 Provider、OpenAI-compatible、Ollama/LM Studio、Proxy/`NO_PROXY`。
4. 能力：SkillHub.cn、Plugin Registry、MCP 测试服务。
5. 渠道：Telegram、QQ、飞书、企微的测试凭据；个人微信不把二维码或会话数据写入 `.env`。
6. 第三阶段：New API management/data endpoint、License endpoint、测试证书/公钥路径；签名私钥不进入客户端 `.env`。
7. 发行：Release feed URL 和公钥；发行签名私钥不进入客户端 `.env`。

实际字段名必须以已有生产代码的环境变量读取点为准。实现前先完成读取点盘点，禁止创建代码不读取的装饰性字段。

## 5. 联调记录和状态规则

`P4-T07` 维护唯一验收矩阵，每行至少记录：

```text
功能编号 | UI 操作 | 权威后端 | 调用链 | 环境 | 成功证据 | 失败证据 | 状态
```

状态只允许：

- `未接线`：UI 仍使用 Mock、固定值、空数组或 `Unsupported`。
- `已接线待联调`：生产调用链存在，尚无真实后端成功证据。
- `本地真实通过`：本机真实 OpenClaw/文件/本地服务已通过。
- `待外部环境`：只缺账号、Windows、物理 U 盘或远程服务。
- `已验收`：真实成功与失败路径都有证据。

不得使用“页面可见”、“typecheck 通过”、“Mock E2E 通过”或“localhost fixture 通过”代替真实接线状态。

## 6. 测试与阶段边界

- 开发机先完成 OpenClaw Gateway、会话、文件、记忆、工具、MCP、日志和可用本地 Provider 接线。
- 外部 Provider 和渠道在凭据填写后按功能包联调；凭据不写入测试 fixture、快照或 Git。
- Windows、Defender、无 Node.js、物理 U 盘、换机、换盘符、拔盘和正式发行判断仍属于 `P3-T08`。
- `P3-T08` 不再承担已知 Mock/未装配/空列表缺口的主体开发；这些缺口必须先在第四阶段收口。

## 7. 不在范围

- 新增原需求未定义的页面或后端能力。
- 用 U-Claw 自建数据库复制 OpenClaw 会话、消息或记忆。
- 将浏览器 Mock 预览改名后作为生产证据。
- 在客户端 `.env` 中保存 License/Release 签名私钥、企业上游 Key 或其他服务端秘密。
