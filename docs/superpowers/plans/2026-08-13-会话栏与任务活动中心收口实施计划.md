# 会话栏与任务活动中心收口实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use test-driven-development to implement each behavior.

**Goal:** 重做工作区会话栏，并把任务活动中心收口到唯一 OpenClaw 数据链路。

**Architecture:** `SessionSidebar` 负责最近、分组、搜索和行菜单状态；`WorkspaceShell` 只负责打开唯一任务中心。Desktop 为专用 Task/Artifact authority 增加只读聚合 fallback，写操作保持 fail closed。

**Tech Stack:** React、TypeScript、Ant Design、Electron IPC、Vitest。

---

### Task 1: 会话栏

- [x] 在 `product/frontend/tests/SessionOrganizer.test.tsx` 增加标题操作、视图切换、搜索和更多菜单测试。
- [x] 运行定向测试，确认因旧界面结构失败。
- [x] 修改 `SessionSidebar.tsx` 与 `global.css`，实现确认的 B 方案。
- [x] 运行定向测试和 frontend typecheck。

### Task 2: 唯一任务中心入口

- [x] 在 `App.test.tsx` 验证会话栏铃铛打开 `TaskArtifactCenter`。
- [x] 由 `WorkspaceShell` 将打开回调传给会话栏。
- [x] 移除未使用的 `TaskActivityCenter` 及重复测试。

### Task 3: OpenClaw 聚合 fallback

- [x] 为原生 RPC 优先、unsupported 后只读聚合、写操作 fail closed 增加 desktop 测试。
- [x] 新增组合 authority，复用 `buildTaskCenterSnapshot`。
- [x] 接入 production wiring。
- [x] 集中运行受影响 desktop 测试、typecheck 和 smoke。
