# 本地应用直达实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 明确“打开应用”指令本地直达，并把完整结果保存在 OpenClaw 会话。

**Architecture:** 桌面主进程维护受控应用索引和严格意图路由。命中后使用 Electron `shell.openPath` 启动，通过官方 `chat.inject` 回写；否则使用既有模型路由。

**Tech Stack:** TypeScript、Electron、Node.js fs、Vitest、OpenClaw Gateway RPC

---

### Task 1: 应用索引和意图识别

- [x] 先写 `product/desktop/tests/local-application-router.test.ts`，覆盖启动扫描、缓存命中、未命中重扫、复合指令回退和路径验证。
- [x] 运行定向测试，确认因模块不存在而失败。
- [x] 新增 `product/desktop/src/local-actions/application-router.ts`，完成最小实现。
- [x] 运行定向测试，确认通过。

### Task 2: OpenClaw 会话回写

- [x] 扩展 Adapter 测试，覆盖内部标签映射为用户角色、普通注入仍为助手角色。
- [x] 运行定向测试，确认新断言失败。
- [x] 扩展历史 Schema 和映射。
- [x] 运行 Adapter 定向测试，确认通过。

### Task 3: 桌面路由接线

- [x] 扩展桌面测试，覆盖本地命中不调用模型、未命中回退模型、回写失败返回错误。
- [x] 运行定向测试，确认失败。
- [x] 在桌面启动 wiring 中组装本地路由、`chat.inject` 和原模型路由。
- [x] 集中运行桌面 typecheck、定向测试和 smoke。

### Task 4: 成功状态归并

- [x] 增加 Adapter 回归测试：`agent.wait=error` 但历史已有成功助手回复时，以历史成功为准。
- [x] 修改兜底归并，只有历史本轮没有成功回复时才返回失败。
- [x] 运行 Adapter typecheck 和定向测试。

## 验证结果

- Desktop 定向测试：62/62。
- Adapter 定向测试：79/79。
- Desktop、Adapter typecheck：通过。
- `git diff --check`：通过。
