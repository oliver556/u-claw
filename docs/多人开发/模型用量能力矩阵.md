# 模型用量能力矩阵

更新时间：2026-08-25

## 1. 状态口径

本矩阵遵守 `docs/多人开发/开发硬性要求.md`：没有 OpenClaw 原版 `method/event/CLI/config` 支撑的能力不进入正式 UI；`Unknown` 不开发；`Blocked` 只能显示明确降级态。

## 2. 矩阵

| # | OpenClaw 原能力 | Bavi-box UI 入口 | 权威调用方式 | 配置来源 | 当前状态 | 验证命令/截图 | 风险 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 会话用量汇总 | 模型页摘要卡、趋势、流水 | Gateway `usage.cost`；usage 页面现有 loader | OpenClaw transcript usage cache；模型 `cost` 配置 | OK | `rg -n "usage.cost" u-claw-app-dev/node_modules/openclaw/dist/control-ui/assets/usage-page-*.js` | 模型 cost 为 0 时只能显示 token 或“本地估算” | 可进入 UI，禁止伪造金额 |
| 2 | Provider billing/status snapshot | 模型页余额/账单状态 | Gateway `usage.status` | Provider usage contract | OK | `rg -n "usage.status" u-claw-app-dev/node_modules/openclaw/dist/control-ui/assets/usage-page-*.js` | 不同 provider 返回字段不一致；可能无余额字段 | 无余额时显示“账单未接入” |
| 3 | 当前文字模型配置 | 模型页“文字对话”卡 | `runtimeConfig` 读取 `agents.defaults.model.primary` | `openclaw.json` | OK | `rg -n "agents.*defaults.*model|runtimeConfig" u-claw-app-dev/node_modules/openclaw/dist/control-ui/assets/config-page-*.js` | 配置 dirty 时不可静默覆盖 | 首版可只读展示 |
| 4 | 当前图片模型配置 | 模型页“图片生成”卡 | `runtimeConfig` 读取 `agents.defaults.imageGenerationModel.primary` / `imageModel.primary` | `openclaw.json` | OK | `rg -n "imageGenerationModel|imageModel" u-claw-app-dev/resources/default-openclaw.json u-claw-app-dev/src/main.js` | 旧配置可能只存在一个字段 | 首版按优先级读取 |
| 5 | 当前视频模型配置 | 模型页“视频生成”卡 | `runtimeConfig` 读取 `agents.defaults.videoGenerationModel.primary` | `openclaw.json`；本机 xAI adapter | OK | `rg -n "videoGenerationModel|xai" u-claw-app-dev/resources/default-openclaw.json u-claw-app-dev/src/main.js` | adapter 限流与上游失败不能伪装为正常 | UI 标注 adapter/限流可能 |
| 6 | 模型管理高级配置 | 模型页“模型管理”按钮 | 导航到 Config/AI Agents models | OpenClaw Control UI config route | OK | `rg -n "modelsSubtitle|models.list" u-claw-app-dev/scripts/patch-openclaw.js` | 高级配置仍是原版结构 | 首版保留跳转 |
| 7 | 模型切换并写回配置 | 模型卡选择弹窗 | `runtimeConfig.patchForm` + `save` + readback | `openclaw.json` | Blocked | 需专门验证三类模型 readback | 错写模型会破坏文本/图片/视频链路 | 不进入首版写入；后续单独切片 |
| 8 | 充值词元/充值记录 | 模型页余额卡按钮 | 待接入 Bavi-box/New API 账单接口或 URL | 远端账单系统 | Blocked | 暂无本仓库权威接口 | 伪金额会误导用户消费决策 | 首版按钮禁用或显示未接入 |

## 3. 数量汇总

- `OK`：6
- `Blocked`：2
- `Unknown`：0
- `Do Not Build`：0

首版开发范围：#1-#6。#7、#8 仅做安全降级或跳转，不做假闭环。

