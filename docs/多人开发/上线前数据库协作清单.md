# 上线前数据库协作清单

更新时间：2026-08-29

## 0. 边界

本文记录上线前我方主导执行、同事提供访问与确认的数据库事项。不得把数据库密码、完整连接串、token 明文、license secret、activation code 明文、USB fingerprint 明文写入本文、Git、日志或截图。

当前协作分两线：

1. staging update check 最小只读权限。
2. prod 激活库 migration、建表与验收数据准备。

## 1. Staging DB 凭证与只读权限

负责人：我方主导；同事提供安全访问、现网路径或必要确认。

目标：

- 修正服务器 `/srv/uclaw-staging32/config/activation.env` 中的 `DATABASE_URL`。
- 或新建只读账号 `uclaw_update_readonly`。
- 给 update check 只读最小权限。

最小读权限范围：

```txt
device_access_tokens
licenses
devices
activation_inventory
```

执行要求：

- `DATABASE_URL` 仅放服务器受限 env，不回传明文到仓库。
- 如新建 `uclaw_update_readonly`，只授予 `SELECT`，不得授予 DDL、写入、grant、schema owner、superuser 权限。
- 若使用现有账号修复连接串，需确认 update check 只使用最小查询，不复用 migration/admin 权限。

同事需先提供或确认：

- staging 服务名与 env 文件路径确认。
- 只读账号名确认，或说明复用哪个非 admin runtime role。

我方执行并产出：

- 修正 `DATABASE_URL` 或创建/校验 `uclaw_update_readonly`。
- 四张表 `SELECT` 权限摘要。
- update check 用该账号连接成功/失败摘要。
- 写入/DDL 拒绝摘要。
- 若失败，记录脱敏错误码和出错阶段。

验收：

- update check 能读取 device token、license、device、inventory 必需字段。
- update check 无法 `INSERT/UPDATE/DELETE` 上述表。
- update check 无法 `CREATE TABLE`、`ALTER TABLE`、修改 `schema_migrations`。

## 2. Prod 数据库准备

负责人：我方主导；同事提供 prod 安全访问、prod env 路径、一次性测试 token 和 migration 5 release/ledger 来源。

目标：

- 修 migration 5 checksum。
- 迁移 `uclaw_activation`。
- 创建 prod 所需表。
- 准备 prod 测试 license/device/token。
- 确认 prod 数据可用于上线前验收。

执行要求：

- 迁移前先备份 prod 数据库，并记录回滚点。
- checksum 修正前先确认 release 包内 migration 5 文件与 prod ledger 的差异来源；不得直接绕过 checksum。
- 使用独立 migration 管理连接执行 DDL；application role 不执行 DDL。
- prod 测试数据必须为上线前验收专用，不复用真实客户 token 或真实客户激活码。
- 验收证据只记录脱敏摘要，不导出 token digest、license secret、New API key envelope 或完整 device fingerprint。

建议执行顺序：

1. 备份 prod DB，记录备份文件、时间、操作者与恢复命令摘要。
2. 对比 migration 5 文件 checksum 与 `schema_migrations` 记录，确认修复策略。
3. 运行 migration，完成 `uclaw_activation` 迁移与 prod 表创建。
4. 验证 `schema_migrations` version/checksum 与当前 release 一致。
5. Seed prod 测试 license/device/token。
6. 用 application role 做只读/业务 DML 验收，并确认 DDL 被拒绝。
7. 用 update check 和激活链路做上线前验收。

同事需先提供或确认：

- prod 安全访问方式，或确认由我方在受控窗口内操作。
- prod env 文件路径。
- prod migration 5 对应 release/ledger 来源。
- prod 测试 `device_id` 和一次性 `device_token`；`device_token` 不进入本文档。

我方执行并产出：

- prod 备份完成摘要与回滚点。
- migration 5 checksum 修复说明。
- `schema_migrations` version/checksum 摘要。
- `uclaw_activation` 迁移完成摘要。
- prod 表存在性摘要。
- prod 测试 license/device/token 的脱敏标识。
- update check 与激活链路验收结果。

验收：

- migration 5 checksum 与 release 一致。
- prod 表齐备，`device_access_tokens`、`licenses`、`devices`、`activation_inventory` 可用于上线前验收。
- prod 测试 license/device/token 可驱动 update check。
- application role 无 DDL 权限。
- 日志与证据不含密钥、明文 token、完整 USB fingerprint 或完整激活码。

## 3. 我方执行动作

同事给出安全访问与必要输入后，我方执行：

- 复验 staging update check。
- 复验 prod update check。
- 复验 prod 测试 license/device/token 状态。
- 补充上线前验收记录。
- 若 staging/prod 权限或 checksum 异常，停止切流。

## 4. 同事已交付给我方的输入

以下为同事回传给我方的执行输入，均不含 secret。`device_token` 仍只允许通过一次性安全通道临时传递。

### Update Check 入口

```txt
staging 当前入口：https://updates.yiyong.me/uclaw/update/check
健康检查：https://updates.yiyong.me/healthz
```

硬更新下载由 R2 承载，阿里云只做控制面，不转发 `runtime.pkg` 大文件。

### 当前 Staging Release

```txt
requiredVersion: 1.0.0
releaseId: v1.0.0
staging R2 production.json: https://pub-8289f643846848528b61bfd5dbf17e43.r2.dev/releases/production.json
staging R2 bucket: u-claw-updates-staging
prod R2 bucket: u-claw-updates-prod
prod R2 public URL: https://pub-4f24fbbe718b4c75955440700c70bdeb.r2.dev
```

### Update Check 客户端入参

不要用 `activation_code`。不要只用 `license_id`。update check 使用 device token：

```http
Authorization: Bearer <device_token>
```

body 示例：

```json
{
  "deviceId": "<devices.device_id>",
  "platform": "win32",
  "arch": "x64",
  "platformKey": "win32-x64",
  "installedVersion": "0.0.0"
}
```

`platformKey` 可选：

```txt
win32-x64
darwin-arm64
darwin-x64
```

### 真实查库鉴权规则

文档中的脱敏验收 SQL 不能替代真实 update check 鉴权。真实鉴权按 token digest、四表状态、时间窗口和关联条件执行：

```sql
FROM device_access_tokens token
JOIN licenses license
  ON license.license_id = token.license_id
 AND license.device_id = token.device_id
JOIN devices device
  ON device.device_id = token.device_id
 AND device.inventory_id = token.inventory_id
JOIN activation_inventory inventory
  ON inventory.id = token.inventory_id
WHERE token.token_digest = sha256(raw_device_token)
  AND token.status = 'active'
  AND license.status = 'active'
  AND device.status = 'active'
  AND inventory.status = 'active'
  AND license.not_before <= now()
  AND license.expires_at > now()
  AND device.device_id = $deviceId
```

### 最小只读权限表

```txt
device_access_tokens
licenses
devices
activation_inventory
```

建议只读账号：

```txt
uclaw_update_readonly
```

建议权限：

```sql
GRANT USAGE ON SCHEMA public TO uclaw_update_readonly;
GRANT SELECT ON TABLE
  public.device_access_tokens,
  public.licenses,
  public.devices,
  public.activation_inventory
TO uclaw_update_readonly;
```

验收要求：

```txt
SELECT 通过。
INSERT / UPDATE / DELETE 拒绝。
CREATE TABLE / ALTER TABLE / 修改 schema_migrations 拒绝。
```

### Staging 候选 Env 路径

```txt
/srv/uclaw-staging32/config/activation.env
```

如果这个 app 用户 `DATABASE_URL` 当前不可用，则修复，或改建 `uclaw_update_readonly` 只读账号。

### Prod 迁移边界

当前仓库中 Cloud API 只确认到：

```txt
u-claw-app-dev/cloud/uclaw-cloud-api/000001_init.sql
```

旧 `product/activation-server` 只确认 migration 1-4。

因此 prod migration 5 的真实来源不能从当前代码仓库确认。必须以 prod 当前 release 包、服务器 migration ledger，或同事手上的 activation 后端 release 为准。不要把 Cloud API 的 `000001_init.sql` 和 prod activation migration 5 混用。

### Prod 仍需现场补齐

```txt
prod 实际 env 路径。
prod migration 5 checksum 差异来源。
prod 测试 device_id。
prod 一次性 device_token。
prod 迁库、seed 测试数据、权限验收结果。
```

`prod device_token` 不写文档、不进 Git、不截图，临时一次性传递给执行验收的人即可。

同事最新澄清：

- 上述 prod 项不是本地已创建好的输入，而是 prod 现场执行后才会产生的交付物。
- 当前同事只能提供规则、SQL 口径、R2 release 信息、update check 入参和验收标准。
- prod 实际 env 路径、migration 5 checksum 来源、prod 测试 `device_id`/`device_token`、迁库 seed 权限验收结果，都需要在 prod 服务器/数据库现场确认或执行后产出。
- 若由我方产出，需要受控 SSH/DB 访问和执行窗口；否则由 prod 现场执行后回传脱敏结果。

## 5. 现场执行结果

执行时间：2026-08-29

### Staging

```txt
update check 只读账号是否可用：是
env 文件路径：/opt/uclaw-update/.env
候选 activation env 路径：/srv/uclaw-staging32/config/activation.env
4 张表 SELECT 是否通过：是
DDL/写入是否拒绝：是
schema_migrations 是否拒绝读取/修改：是
公网 update check 是否业务命中：是，假 token 返回 license-device-token-denied，HTTP 200
当前 release：requiredVersion=1.0.0，releaseId=v1.0.0
```

说明：

- `/srv/uclaw-staging32/config/activation.env` 内 app `DATABASE_URL` 现场验证为密码认证失败。
- 已将 `uclaw-update.service` 改为使用 root-only URL file 和 `uclaw_update_readonly`。
- URL file：`/opt/uclaw-update/secrets/update-readonly-database-url`，权限 `root:root 600`。
- update env：`/opt/uclaw-update/.env`，权限 `root:root 600`。
- 回滚备份：`/root/uclaw-update-backup-20260829-141633/`、`/root/uclaw-update-backup-20260829-141709/`。

### Prod

```txt
prod 迁库是否完成：是
prod env 文件路径：/etc/uclaw-activation.env
prod migration 5 checksum 来源：/opt/uclaw-activation/current/migrations/005_release_policy.sql
prod 测试 device_id：51f85535-c3fe-4608-bcdd-402a2e58f097
prod device_token：不写文档；SSH 到服务器后从 root-only 文件现场读取
update check SQL 是否命中：是
4 张表 SELECT 是否通过：是
DDL/写入是否拒绝：是
```

prod migration ledger 已到 version 6；version 5 checksum 与当前 release 文件一致：

```txt
5:b00ee6b9d1f5eb6119ad9a21a5f9d2c2c1ba44577a8df85833e9223bee226c6d
6:4fd11d94d607d5fe6dd2c382b18635b0250db32d70860be88b85e35c46d5990a
```

prod 测试 token 交付方式：

```txt
token 文件：/root/uclaw-prod-update-test-token-20260829T061958Z.txt
metadata 文件：/root/uclaw-prod-update-test-metadata-20260829T061958Z.txt
rollback SQL：/root/uclaw-prod-update-test-rollback-20260829T061958Z.sql
seed 前备份：/root/uclaw-prod-pre-update-test-seed-20260829T061958Z.sql
```

prod 最小只读账号：

- `uclaw_update_readonly` 已授予 prod `uclaw_activation` 四表 `SELECT`。
- prod readonly URL file：`/opt/uclaw-update/secrets/update-readonly-prod-database-url`，权限 `root:root 600`。
- 验证通过：`device_access_tokens` / `licenses` / `devices` / `activation_inventory` 可 `SELECT`，`UPDATE` 与 `CREATE TABLE` 被拒绝。

暂未切 prod：

- prod R2 `https://pub-4f24fbbe718b4c75955440700c70bdeb.r2.dev/releases/production.json` 当前返回 `404 text/html`。
- 阿里云服务器未发现 `wrangler` / `aws` / `rclone` / `s3cmd` 上传工具或 R2 凭据。
- 因 prod release manifest 未就绪，公网 `updates.yiyong.me` 仍保持 staging release 与 staging DB 配置。

## 6. 同事最终交付输入格式

执行完成后，按下面格式记录交付结果。除 `prod 测试 device_id` 可给完整 UUID 外，禁止在文档、Git、群聊长期记录或截图中写入明文密码、完整连接串、prod `device_token`、license secret、New API key 或完整 USB fingerprint。

### Staging

```txt
update check 只读账号是否可用：是/否
env 文件路径：<服务器本机路径，例如 /srv/uclaw-staging32/config/activation.env>
4 张表 SELECT 是否通过：是/否
DDL/写入是否拒绝：是/否
备注：<脱敏错误码或执行说明，可为空>
```

### Prod

```txt
prod 迁库是否完成：是/否
prod 测试 device_id：<完整 UUID>
prod device_token：不写文档；通过一次性安全通道临时传递
prod env 文件路径：<服务器本机路径>
update check SQL 是否命中：是/否
备注：<脱敏错误码、回滚点或执行说明，可为空>
```

`prod device_token` 临时传递要求：

- 只发给执行上线前验收的人，不进入本文档和 Git。
- 优先使用一次性密文消息、受限密码管理器共享项或 SSH 到服务器后现场读取。
- 传递后记录“已传递/已使用/已作废”的状态即可，不记录 token 明文。
- 验收完成后按 prod 策略保留、轮换或作废测试 token。

## 7. 当前状态

- staging DB：已切到 `uclaw_update_readonly`，公网 update check 假 token 业务拒绝返回正常。
- prod DB：迁库、权限、测试 license/device/token seed 与 SQL 命中均完成。
- prod 切换：暂缓，等待 prod R2 manifest 发布或提供 R2 上传凭据/工具。
- 本项目：我方主导复验与切 prod 判断；不把任何 secret 明文写入仓库或文档。
