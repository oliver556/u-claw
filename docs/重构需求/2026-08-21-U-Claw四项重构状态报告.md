# U-Claw 四项重构状态报告

日期：2026-08-21（2026-08-22 追加商业链口径）
项目：`/Users/jamison/Document/300_学习/320_code/390_ai_agent/u-claw`
分支：`codex/uclaw-refactor-final`
性质：状态报告；已从下载目录复制到重构需求目录，并按后续集成事实校准口径

## Executive Summary

当前不是“四项重构全部完成”。准确状态：

- 产品主线已从“需求冻结 / 合同测试”推进到“远端临时验收分支集成 + `product.yml` successful + Windows final runtime artifact 已生成”。
- Shell/Gateway、动态模型 relay、OpenClaw Provider、OpenClaw-only routing、R2 runtime、Bootstrap 强制版本、发布流水线骨架，都已有远端集成 commit。
- Win final runtime / 物理 U 盘门禁已 PASS，不阻塞最终冻结。macOS / 双系统 U 盘目录仍是补充需求，未落代码。
- `final-windows-runtime` 已有两个 successful artifact：
  - 旧 artifact：run `32470239142`，head `38e86399be37bd51bb9b26d7797b40980c5cac88`，artifact `9442474022`，digest `sha256:20d1096e0b952f3f017aefc1c770b45f7f56f72b65a4be30ede4304da49e9cc7`。该 artifact 已确认 runtime-provenance mismatch，不能继续门禁。
  - Win PASS artifact：run `32473646655`，head `52d50fee0e8227d5fed2dcc673109fb2bf9b7174`，artifact `9443717301`，digest `sha256:146681cd27f21f959f599ad8235581c1cd35842fa90dbb86d606957906736f5f`。
- Win 门禁使用 artifact `9443717301` 已通过。不要切回 `9442474022`，也不要混入 90 后续 push 产生的新 artifact。
- 43 OpenClaw 能力边界、46 商业模型运行配置、44 补充文档并入总纲，均已通过 90 串行集成并 push 到远端。
- 当前远端 `codex/uclaw-refactor-final` HEAD：`c9a93ccf98c452a20bff99b750353334af1f2a28`。
- Win 商业链 E2E 已在 artifact `9450743485` 上复测并失败，失败点集中在生产商业链，不是 runtime artifact：credential 仍为 v1、缺 `deviceTokenId`、`qwen3-max` / `deepseek-v4-flash` / Tool Calling / 图片编辑 502、OpenClaw CLI 无法 resolve gateway secrets。
- Mac 侧已集成并部署两项商业链返工：`00a26715` 放宽商业 chat response extension 校验，`c9a93ccf` 让 release policy 配置在生产缺省时降级为 unavailable 而不是阻断 activation/model proxy 启动。
- 阿里生产 `activation-server` 已切到 `c9a93ccf` Linux amd64 binary，public `/health/ready` PASS；生产 DB `schema_migrations` 1-6 已匹配当前代码，005 release policy 表缺失已补齐。
- 新 Win 复测授权已生成并下发，当前等待 Win Codex 商业链复测结果。Win 仍不得本地 rebuild final runtime，仍使用 artifact `9450743485`。
- runtime artifact 线冻结正确。后续商业 E2E / 双系统 / release gate 未要求解冻前，不碰：
  - `product/packaging/final-windows-runtime.mjs`
  - `product/tests/packaging/final-windows-runtime.test.mjs`
  - `.github/workflows/product.yml`
  - `product/packaging/runtime-app` lock
  - Node/npm/Electron/OpenClaw runtime pins

## 口径修改集群（2026-08-21 后续校准）

本节覆盖本报告初版中的旧事实。

- Win artifact 口径：从旧 `9442474022` 改为当前 `9443717301`。旧 artifact provenance mismatch 证据保留，不再门禁。
- 集成 HEAD：从 `52d50fee0e8227d5fed2dcc673109fb2bf9b7174` 推进到 `a5c5d989881d9fe147215f7d1baf6b1b6b538dbc`。
- 43 OpenClaw 能力边界：已完成并集成；默认自然语言入口进入 OpenClaw `chat.send`，不再走 `local-actions`、commercial image direct router 或 OpenClaw image CLI 直连注入路径。
- 46 商业模型运行配置：已完成并集成；credential v2 写入 `endpoint/deviceTokenId/model/deviceToken`，默认模型优先 `gpt-5.5`，OpenClaw commercial primary 使用 `uclaw-commercial/<defaultModel>`，BYOK 与商业凭据隔离。
- 44 文档并入总纲：已完成并集成；三份 2026-08-21 补充决策已在 `docs/重构需求/` 跟四份重构文档放在一起。
- 90：已串行集成并 push；`product.yml` 未自动触发新 run，当前 Win 不需要换包。
- Win final runtime gate：已 PASS。Windows 11 Pro 10.0.22621 x64，普通路径、空格路径、中文路径、物理 U 盘路径均通过；runtime-provenance、PE x64、app.asar、OpenClaw package 均通过。
- 57/58/59 商业链返工：57 补强 PostgreSQL 006 alias backup/restore gate；58 修商业模型代理 502 误判；59 修生产缺少 release policy env 时 activation/model proxy 启动被误阻断。
- 生产部署口径：live server commit `c9a93ccf98c452a20bff99b750353334af1f2a28`，binary sha256 `4a7776bacc5ef83153466492e41c95f894ac710d95ff6386144ae9c125642e55`，`https://121.41.89.103/health/ready` PASS。
- Win 商业链复测口径：使用 runtime artifact `9450743485`，run `32492933120`，head `ff8dd6a565d25ac7e2351b9d29281ac6335da0ab`，zip sha256 `967bba8e16bd4a7d2caae7cd0ed5afacd81e9f2a477f046dec67a66fd820da7d`；等待新授权复测回传。

## 阶段状态表

| 阶段 | 原要求 | 当前状态 | 已知证据 | 未完成项 / 风险 | 下一步 |
|---|---|---|---|---|---|
| 阶段 -1：实施前证据门 | 启动基线、R2 物理证据、bridge 审计、失败复现、秘密边界 | partial | 需求文档已冻结；后续多批测试/CI 已补 | 原始 R2 物理矩阵、macOS、低速 U 盘证据不完整 | 保留为最终验收证据缺口 |
| 阶段 0：冻结测试合同 | 先写失败测试，冻结 Gateway/聊天/模型/秘密合同 | done | `bdc8884c`、`518db5d9`、`986a4f64`；`product.yml` success | 真机覆盖仍要后补 | 可作为已集成基础 |
| 第一批 A：Shell / Gateway 解耦 | Shell 先显示；Main 单一拥有 Gateway；attempt/能力状态/重试 | done / needs-evidence | `8fdb275f` | 连续重试十次、物理故障矩阵缺最终报告 | 后续故障矩阵验证 |
| 第一批 B：动态模型目录与服务端中转 | New API server-only；动态模型；SSE/tool/image edits | partial-integrated | `fc56cda7`、`409e8fe9`、`7aa31ade`；补充草案黑盒 `gpt-5.5/deepseek/qwen/image` HTTP 200 | Tool Calling、`/images/edits`、真实 OpenClaw 链需最终证据 | 商业 E2E 验证窗口 |
| 第一批 C：聊天 mapper 与生命周期测试 | live/history/recovery 唯一 mapper；切会话不 abort | done / needs-evidence | `8b7576aa`、后续 frontend 稳定性 commits | 真机 live/history 图片一致性要证明 | Win/商业 E2E 验证 |
| 第二批：OpenClaw 商业 Provider | `endpoint + deviceToken`，SecretRef，token rotate → Gateway restart | done / needs-evidence | `c0c6857f`、`113aba33`、`e731a9e4`、`60b5339e` | Token 轮换真机 readback 证据不足 | 单独 validation |
| 第三批：商业新链真机端到端 | UI → OpenClaw → commercial provider → 中转 → New API | partial | `a4c1408e`、`8a241086`、`4e6713eb`；运行配置草案黑盒通过 | 真机端到端矩阵未完成 | 新开 validation |
| 第四批：切换 OpenClaw 唯一聊天链 | 删除普通商业直连；CI 禁止直连 | integrated / needs-evidence | `6de3cc25`、`98ce312e` | 默认自然语言入口已收口；图片、视频、电脑、浏览器、MCP、Skill、Plugin、渠道仍缺最终 runtime 证据 | 商业 E2E / 能力边界 validation |
| 第五批：R2 runtime | `runtime.pkg` 只运输；宿主内容寻址 runtime | integrated / Win-gate-pass | `360c219a`；runtime provenance：Node `24.15.0`、npm `11.12.1`、Electron `40.10.6`、OpenClaw `2026.7.1-2`；Win artifact `9443717301` 门禁 PASS | macOS target 未实现 | 商业 E2E / 双系统后续 |
| 第六批：Bootstrap、强制更新与向前回滚 | 每次在线版本门禁；无客户端 rollback；服务端更高 sequence 回滚 | partial-integrated | `260d5f0`、`25e8b1dd`、`da935969`、`54b85394` | 真实 CDN/pointer/失败恢复门禁未完成 | 发布前 validation |
| 第七批：唯一商业发布流水线 | `product/` 唯一输入；final runtime smoke；artifact；CDN 回读 | partial | `d95cde0f`；`product.yml` success；Win final runtime artifact 已通过真机/物理 U 盘门禁 | 双系统制盘目录、macOS、正式 release gate 未完成 | 商业 E2E 后继续 |
| 第八批：物理 U 盘最终发布门禁 | Win10/11、macOS、U 盘、更新、License、聊天业务矩阵 | partial / Win-runtime-pass / commercial-retest-pending | Windows 11 + 物理 U 盘 final runtime 门禁 PASS；商业链旧复测 FAIL；Mac 侧 58/59 已部署生产 | macOS、商业业务矩阵、更新/License/CDN 故障矩阵未完成；当前等待 Win 新授权复测 | 等 Win 商业链复测 PASS/FAIL |

## 主线状态表

| 主线 | 当前状态 | 分类 |
|---|---|---|
| Shell / Gateway 生命周期 | 已集成，缺最终真机故障矩阵 | 已完成并集成；缺物理证据 |
| 动态模型目录与商业凭据 | 核心已集成；`gpt-5.5` 默认和 credential v2 合同已落代码；`allowed_models` 策略仍需真实服务验证 | 已集成；缺真机/服务端证据 |
| OpenClaw Provider / 唯一聊天链 | Provider 和 OpenClaw-only routing 已集成 | 已完成并集成；缺真机证据 |
| 商业新链真机端到端 | 代码线有成果；Win 旧复测 FAIL；58/59 已部署生产；等待新授权复测 | 阻塞中，等待 Win 商业链复测 |
| R2 runtime / runtime.pkg / final-windows-runtime | CI artifact 已生成；Win 真机 / 物理 U 盘门禁 PASS | Win final runtime 已过；macOS runtime 未完成 |
| Bootstrap / 强制更新 / forward rollback | 客户端/服务端/发布证明已有集成 | partial；生产级门禁未完成 |
| 发布流水线 / artifact / U 盘门禁 | `product.yml` success；Win final runtime artifact 门禁 PASS | Win runtime 闭环；release/CDN 未完成 |
| 双系统 U 盘目录结构 | 补充文档已入重构需求目录；代码未落 | 需求已更新但未落代码 |
| OpenClaw 能力边界红线 | 补充文档要求自然语言只进 `chat.send`；默认聊天链已收口 | 已集成；缺真机/工具链证据 |
| 商业模型运行配置补充 | New API key server-only、模型列表、默认模型、credential v2 合同已落代码 | 已集成；缺真实凭据 E2E |

## 已完成并集成

- Shell / Gateway 解耦基础：`8fdb275f`
- 动态商业模型 relay：`fc56cda7`
- canonical mapper / session recovery：`8b7576aa`
- commercial OpenClaw token lifecycle：`c0c6857f`
- provider secret scrub / streaming lifecycle 修复：`113aba33`、`60b5339e`
- OpenClaw-only model routing：`6de3cc25`
- commercial image turns through OpenClaw：`a4c1408e`
- R2 content-addressed runtime：`360c219a`
- signed release policy / forward rollback：`25e8b1dd`
- signed bootstrap release gate：`260d5f0`
- CDN readback gate：`da935969`
- final Windows runtime CI 构建链：`d95cde0f`
- Windows spawn EINVAL 等价修复：`e6039c7`
- final Windows artifact hidden files 上传修复：`52d50fee`
- OpenClaw 能力边界收口：`98ce312e`
- 商业模型运行配置落地：`7e543dd7`
- 三份 2026-08-21 补充文档并入总纲：`a5c5d989`
- PostgreSQL 006 alias backup/restore gate：`36100bce`
- 商业模型代理 response extension 兼容：`00a26715`
- activation-server release policy config 降级启动：`c9a93ccf`

## 已集成但缺真机 / 物理证据

- Shell 可见 + Gateway 后台能力降级
- Gateway 重试十次无重复进程 / timer / IPC handler / orphan process
- OpenClaw Provider token rotation readback
- 商业新链真实文本多帧 SSE、Tool Calling、图片生成、图片编辑。Win 旧复测中文本/上下文/后台会话/图片生成 PASS，但 credential v2、Tool Calling、图片编辑、qwen/deepseek、OpenClaw CLI FAIL；58/59 部署后需复测。
- 切会话不 abort，后台 run 完成后 history 恢复一致
- R2 runtime 首次安装和 warm startup
- 强制版本服务失败、CDN 故障、License 故障、空间不足、拔盘、换盘符

## Win final runtime / 物理 U 盘门禁结果

结论：PASS。不阻塞最终冻结。本轮没有本地 rebuild `final-windows-runtime`，只验证 GitHub Actions successful run 产出的 artifact。

- Windows：Windows 11 专业版，Version `10.0.22621`，Build `22621.4317`，x64。
- Artifact：run `32473646655`，artifact `9443717301`，zip sha256 `146681cd27f21f959f599ad8235581c1cd35842fa90dbb86d606957906736f5f`，run head `52d50fee0e8227d5fed2dcc673109fb2bf9b7174`。
- Provenance：`runtimeKind=final`，`buildHost=win32/x64/windows-x64`，Node `24.15.0`，npm `11.12.1`，Electron `40.10.6`，OpenClaw `2026.7.1-2`。
- Inventory：`fileCount=32104`，`unpackedBytes=667815105`，`treeSha256=78c1510750ff89ba322ab4cfadc08d215403f3caedc8c7913ec6c5f59a046cd0`，三项均 PASS。
- Runtime 内容：`electron.exe` 为 `PE32+ x64 machine=0x8664`；`resources/app.asar` size `6523124`；`openclaw` package version `2026.7.1-2`。
- 路径门禁：普通 Windows 本地路径 PASS，带空格路径 PASS，中文路径 PASS，物理 U 盘路径 PASS。
- 物理 U 盘：`D:`，VolumeName `U-CLAW`，FileSystem `exFAT`，DriveType `2`，Size `31452004352`，FreeSpace `31197757440`。
- 证据目录：`C:\uclaw-final-runtime-gate-fast-20260821-200208`。旧 artifact 失败证据保留在 `C:\uclaw-final-runtime-gate-fast-20260821-193239`。
- 注意：`gate-report.json` 保留第一次 D 盘短暂不可用 FAIL；最终有效 U 盘证据是 `validate-usb.stdout.json`。

## final-windows-runtime artifact 门禁结果

### 旧 artifact（失败证据保留，不再门禁）

- workflow：`product.yml`
- run id：`32470239142`
- conclusion：`success`
- head：`38e86399be37bd51bb9b26d7797b40980c5cac88`
- artifact id：`9442474022`
- artifact name：`final-windows-runtime`
- digest：`sha256:20d1096e0b952f3f017aefc1c770b45f7f56f72b65a4be30ede4304da49e9cc7`
- provenance：
  - `runtimeKind: final`
  - `commitSha: 38e86399be37bd51bb9b26d7797b40980c5cac88`
  - `os: win32`
  - `arch: x64`
  - `runner: windows-x64`
  - Node `24.15.0`
  - npm `11.12.1`
  - Electron `40.10.6`
  - OpenClaw `2026.7.1-2`
  - treeSha256 `78c1510750ff89ba322ab4cfadc08d215403f3caedc8c7913ec6c5f59a046cd0`
  - fileCount `32104`
  - unpackedBytes `667815105`
- 失败结论：GitHub artifact zip 比 `runtime-provenance.json` 声明少 `160` files / `317,684` bytes；根因是 `actions/upload-artifact@v4` 默认不包含 hidden files/dotfiles。
- 当前用途：只保留失败证据，不再继续普通路径、空格路径、中文路径或物理 U 盘路径门禁。

### 当前 Win PASS artifact

- workflow：`product.yml`
- run id：`32473646655`
- conclusion：`success`
- head：`52d50fee0e8227d5fed2dcc673109fb2bf9b7174`
- artifact id：`9443717301`
- artifact name：`final-windows-runtime`
- digest：`sha256:146681cd27f21f959f599ad8235581c1cd35842fa90dbb86d606957906736f5f`
- 变更：`fix(product): preserve hidden files in runtime artifact`
- artifact provenance 本地固化已通过：treeSha256 / fileCount / unpackedBytes 全部一致。
- 当前用途：已完成 Windows 真机 / 物理 U 盘 final runtime 门禁，结果 PASS。

## 需求已更新后的落地状态

### 双系统 U 盘目录

补充口径：

```text
U-Claw/
├─ U-Claw.exe
├─ U-Claw.app/
├─ app/packages/win-x64/runtime.pkg
├─ app/packages/macos-arm64/runtime.pkg
├─ data/
└─ .uclaw/license/
```

关键变化：

- 原 Windows 首版结构不等于双系统最终发行结构。
- Windows/macOS 平台 runtime、manifest、current、install-state 独立。
- `data/`、`.uclaw/license/` 共享。
- macOS USB 指纹、跨系统授权、Gatekeeper/notarization 未实现。

### OpenClaw 能力边界

补充红线已首轮落代码：

- 所有自然语言输入只进入 OpenClaw `chat.send`。
- U-Claw 不做自然语言意图识别分流。
- 图片、视频、电脑、浏览器、MCP、Skill、Plugin、渠道，都由 OpenClaw tool chain 决策。
- `local-actions` 只能作为明确 UI 按钮动作，不能拦截默认聊天输入。
- 已集成证据：`98ce312e`。剩余是最终 runtime 和工具链真机证据，不是默认聊天链代码收口。

### 商业模型运行配置

补充草案已首轮落代码：

- New API key server-only。
- U 盘不保存 New API key。
- U 盘只保存 `endpoint`、`deviceToken`、`deviceTokenId`、默认模型。
- 默认文本模型建议 `gpt-5.5`。
- 模型列表以 `/model-api/v1/models` 为准。
- 短期保留服务端 `allowed_models`，不把 New API 全量模型无边界透传给客户。
- 已集成证据：`7e543dd7`。剩余是商业真实凭据 E2E、`allowed_models` 服务端策略、usage/billing 证据。

### 商业链 E2E 与生产部署

Win 商业链旧复测结论：FAIL，阻塞最终冻结。失败不在 runtime artifact：

- artifact `9450743485` runtime 四路径 PASS，未本地 rebuild。
- 激活使用旧生产服务时 credential 仍为 `schemaVersion=1`，缺 `deviceTokenId`。
- `qwen3-max`、`deepseek-v4-flash`、Tool Calling、图片编辑返回 `502 UPSTREAM_UNAVAILABLE`。
- OpenClaw CLI 商业链报 `failed to resolve secrets from the active gateway snapshot`。

Mac 侧返工与部署：

- `00a26715` 修复 model proxy 过窄响应校验，允许上游返回商业 chat response extensions，避免把有效响应误判为 502。
- `c9a93ccf` 修复 activation-server 在缺少 release policy env 时启动失败；release policy 缺省只让 `/v1/client-policy` 和 admin publish/forward rollback 返回 unavailable，不阻断 `/v1/activations` 和 `/model-api/*`。
- 生产 DB 修复 `schema_migrations` version 5 ledger/DDL 不一致，补齐 `production_releases` 和 `production_release_state`；ledger 1-6 与当前代码 checksum 匹配。
- 生产 live binary sha256：`4a7776bacc5ef83153466492e41c95f894ac710d95ff6386144ae9c125642e55`。
- public health：`https://121.41.89.103/health/ready` PASS。
- 新一次性授权已生成并下发给 Win，当前状态：`waiting-for-win-commercial-retest`。

## 风险与冻结区

### 冻结区

Win final runtime / runtime.pkg 线当前冻结。除非 Win 门禁失败或 final runtime 再失败，否则不要改：

- `product/packaging/final-windows-runtime.mjs`
- `product/tests/packaging/final-windows-runtime.test.mjs`
- `.github/workflows/product.yml`
- `product/packaging/runtime-app` lock
- Node/npm/Electron/OpenClaw runtime pins

### 风险

- artifact 选择风险：Win 当前固定用 `9443717301`。不要切回旧 `9442474022`，也不要混入 90 后续 push 可能产生的新 artifact。
- 补充文档状态：3 份 2026-08-21 补充文档和本状态报告已在 `docs/重构需求/`，随总纲进入当前文档基线。
- 完成度误判风险：CI success 不等于四项完成；物理 U 盘、macOS、CDN/pointer、商业 E2E 仍是门禁。
- 本地工作树脏：当前 `main` 有多处未提交/未跟踪改动。整理报告未使用这些改动当完成证据。
- 商业链风险：若 Win 新授权复测仍 FAIL，优先按 credential v2/deviceTokenId、OpenClaw gateway secrets snapshot、上游 502 三类分诊；不要解冻 runtime packaging。

## 后续窗口清单

| 顺序 | 任务标题 | 类型 | 依赖 Win 门禁 | 碰冻结区 | 推荐归属 |
|---|---|---|---|---|---|
| 1 | Win 物理 U 盘门禁：校验 artifact `9443717301` | validation | 已完成 | 否 | Win 验证 |
| 2 | Win 商业链 E2E 新授权复测 | validation | 正在进行 | 否 | Win 验证 |
| 3 | 商业链复测失败分诊返工 | code/ops | 依赖 Win 复测 FAIL | 否，除非证据指向 runtime | 90 集成 + Mac ops |
| 4 | 双系统 U 盘目录实施计划和代码实施 | general/code | 建议等商业链复测稳定 | 可能碰 packaging，需单独解冻 | 90 集成 |
| 5 | 发布流水线最终门禁：CDN readback、pointer switch drill、正式制盘 | validation | 依赖商业 E2E / 双系统 | 可能 | 90 + 发布验证 |
| 6 | 生产发布审批与 release 执行 | release/validation | 依赖 release gate PASS | 是 | 主调度 + 90 |

## 推荐先后顺序

1. Win 物理 U 盘门禁已 PASS，冻结 runtime artifact 线，继续普通开发。
2. 当前优先等待 Win 使用新授权复测商业链；Mac 不空等，可做 docs/checklist 或不碰 activation/modelproxy/runtime 的普通开发。
3. 若 Win 商业链 PASS，进入双系统 U 盘目录实施和 macOS target。
4. 若 Win 商业链 FAIL，按 credential v2/deviceTokenId、OpenClaw gateway secrets snapshot、上游 502 三类最小返工。
5. 双系统基础稳定后，做 release/CDN/pointer/rollback 最终门禁。
6. release gate PASS 后，进入 production 发布审批和执行。

## 给主调度的建议

现在可以继续普通开发，但只能在非冻结区、非 final runtime artifact 线推进。

已不再等待 Win runtime 门禁，但正在等待 Win 商业链复测。仍需冻结的线：

- final runtime artifact 线
- runtime/pkg/pins
- `product.yml`
- 最终 release gate

可以并行的线：

- Win 商业 E2E 结果观察与失败分诊
- 后续双系统 U 盘实施方案准备

下一步最优先：

> 等 Win 新授权商业链复测结果；runtime artifact 线保持冻结，不因 Win runtime PASS 顺手改 pins / packaging / product.yml。
