# 激活码长期令牌模型代理 VPS 验收

日期：2026-08-14
状态：`partial-pass` / `not-release-ready`
VPS：`121.41.89.103`
分支：`codex/activation-final-local-integration-20260813`
release：`e84f5ae858018b4775d2307561abcdbdf65ed399`

## 结论

VPS migration、双实例滚动部署、IP HTTPS、激活单字段协议、长期 device token 代理鉴权、公网 allowlist、限流、不可路由上游脱敏和逐实例停机接管通过。

发布门禁仍未关闭：域名状态为 `BLOCKED_DOMAIN`，`license.yiyong.me` 的 A/AAAA 均为 NXDOMAIN，域名证书和域名 health 未通过；独立 New API 未提供，未执行真实模型对话；正式 Electron、Windows、Defender 和物理 U 盘未验。

新激活响应按合同返回 `https://license.yiyong.me/model-api/`。当前激活本身可经 IP HTTPS 完成，但客户端随后访问该域名会在 DNS 阶段失败。IP 入口只保留兼容和回滚能力，不替代新产物 endpoint。

## 证据约束

- 本报告只记录 git SHA、checksum、HTTP status、request/support ID、证书元数据和脱敏摘要。
- activation code、device token、startup secret、operator credential 和 New API key 未进入终端输出、Git、报告或 service journal。
- 测试明文仅在 VPS root-owned `0600` 验收文件中处理；临时 operator credential、New API key 和扫描 pattern 已清除。
- 失败备份目录 `20260814T075910Z` 保留，未删除、复用或伪装为成功备份。

## 备份与制品

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 成功备份 | pass | UTC `20260814T080046Z`；manifest SHA-256 `328f42c8f60c58a272456bbc700a4462aa1f7cb9e6c5b28ba87cf0aa42f59be5`；15/15 entries 经 `sha256sum -c` 全部通过；外部 append-only ledger 已验证 |
| manifest 内容 | pass | PostgreSQL custom `activation.dump`/`activation.dump.list` 2；`uclaw-activation.env` 1；`Caddyfile` 1；`caddy.service`、`postgresql.service`、`uclaw-activation@.service` 3；server/admin binary snapshots 4；migration ledger `schema-migrations.txt` 1；metadata/reference 3，共 15 项 |
| PostgreSQL dump | pass | `activation.dump` 识别为 `PostgreSQL custom database dump - v1.14-0`；`pg_restore --list` 成功；独立 restore list 含 66 entries |
| server artifact | pass | SHA-256 `333deb46009b4529c3afbcf63e14c932299b99ea1c00952a1712d15f5bd15156` |
| admin artifact | pass | SHA-256 `3745dbdf91449f09130c0ddfb1c6ac167a933b3680f0420f3f33beb39da727e4` |
| current release | pass | `/opt/uclaw-activation/current` 与 `/usr/local` 固定链接均指向 release `e84f5ae858018b4775d2307561abcdbdf65ed399` |
| rollback binary | pass | 旧 binary 在 migration 4 schema 上 canary `/health/ready` 为 `200`；未运行旧 migrator，未回滚 schema |

## Migration

Migration role 已应用 version 4。ledger version 1-4 与 release migration 文件 checksum 全部匹配：

| Version | SHA-256 | 结果 |
| --- | --- | --- |
| 1 | `2579a86ee7fbc45cbc2f0d523a539159dc74edef8413f8e1c09c253bc51e79b3` | match |
| 2 | `a2696aef259d91d028dff4adc2eab2e5724d93f70d82f4548d5673b55795357f` | match |
| 3 | `2efc9c5ab6ea5e57eaa90591cd99d8ec7d6021ace265023cf0fe2a72e9fa9f48` | match |
| 4 | `86d4488f412aec8bd13fd64cb873c731a2cbbb164a8792c9adf2caf47217e96f` | match |

结构与权限结果：

- `device_access_tokens`、`model_proxy_admissions` 存在。
- 新增 5 个 constraints、2 个 indexes 均存在。
- app role 可 SELECT migration ledger；INSERT、UPDATE、DELETE 与 DDL 实测均以 SQLSTATE `42501` 拒绝。
- migration owner 和 default privileges 符合 runbook。
- migration password 已原子 finalize；新认证成功，旧认证失败；旧恢复副本保留。

## 部署与网络

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 四服务 | pass | `postgresql`、`caddy`、`uclaw-activation@18081`、`uclaw-activation@18082` 最终均 active |
| 双实例 | pass | `18081`、`18082` 均运行新 server binary；loopback 与公网 IP health 均为 `200` |
| live activation env | pass | `/etc/uclaw-activation.env` SHA-256 `36e272b0119ddd7e7e2b61197f55b6dc728a830bbf1e9f15a8494088e1df20f3`；root:root、mode `0600`、link count `1`；`PUBLIC_MODEL_ENDPOINT=https://license.yiyong.me/model-api/`、`NEW_API_ALLOWED_HOSTS=unavailable.invalid` |
| systemd template | pass | `/etc/systemd/system/uclaw-activation@.service` SHA-256 `d4e737a096a326b5390b61018d295b41470e150f31cf4a862e8662f84e6e4bfe`；root:root、mode `0644`；`EnvironmentFile=/etc/uclaw-activation.env`、`Environment=LISTEN_ADDRESS=127.0.0.1:%i` |
| ExecStart 与实例装配 | pass | `ExecStart=/usr/local/bin/uclaw-activation-server` 解析到 release `e84f5ae858018b4775d2307561abcdbdf65ed399`；live process `LISTEN_ADDRESS` 分别为 `127.0.0.1:18081`、`127.0.0.1:18082`，对应 listener 与 loopback health `200` |
| Caddy | pass | config SHA-256 `0ae75115d5e0c08ca6f74a8feff5b84f4dbf05f1aae176b49afe3190d850ca5d`；`caddy validate` 成功 |
| 公网路由 | pass | `/health/ready` 为 `200`；`/metrics`、`/internal/test`、`/`、`/admin`、`/v1/admin/test`、`/model-api/internal/test` 均为 `404` |
| 监听隔离 | pass | app `18081/18082`、PostgreSQL `5432`、Caddy admin `2019` 仅 loopback；公网仅 Caddy `80/443` 和 SSH `22` |

IP certificate：

- issuer：Let's Encrypt `YE1`
- SAN：`IP Address:121.41.89.103`，critical
- validity：`2026-08-13 10:58:53Z` 至 `2026-08-20 02:58:52Z`
- SHA-256 fingerprint：`B9:12:79:1D:05:42:62:35:29:76:D3:CC:28:D3:2E:3C:72:45:BC:E0:65:FB:D7:8E:22:8B:7D:06:B7:47:B9:2B`

域名门禁：

- 状态：`BLOCKED_DOMAIN`。
- `license.yiyong.me` A/AAAA 均无法解析；resolver 状态为 unresolved。
- Caddy ACME journal 明确记录 A 和 AAAA `NXDOMAIN`，证书签发失败。
- `https://license.yiyong.me/health/ready` 在建立连接前失败；无域名 SAN、issuer、dates 或 health 成功证据。
- `PUBLIC_MODEL_ENDPOINT` 保持合同值 `https://license.yiyong.me/model-api/`；不得把 IP endpoint 误记为域名验收通过。

## 激活协议

测试库存使用全新生成记录和不可路由 New API mapping。所有 secret 由 VPS root 进程直接读写 root-only 文件。

| 场景 | HTTP | 结果 | request/support ID |
| --- | --- | --- | --- |
| 错误激活码 | `400` | `ACTIVATION_INVALID`；库存仍 prepared，无 device/license | `req_9aeaaa907976c9abbb7326de8489c764` / `ACT-INV-001` |
| 正确激活 | `200` | active；response root 无 username；builtin credential 无 username/expiresAt | `req_98e70922cad9466d87b559576d3b2a5b` |
| 同 idempotency 恢复 | `200` | activation/device/license/token material 与首次结果相同 | `req_41c531546ba2ff02baf9053d27c2f25f` |
| 第二设备指纹 | `409` | `ACTIVATION_CODE_ALREADY_BOUND` | `req_874c87dab6975ef8ecf37f2c01313156` / `ACT-BIND-001` |
| artifact commit | `204` | generation 1 commit 成功 | `req_421c7aa152b3bcbc0739ec9724e59e44` |

写盘检查：

- `builtin-model-credential.v1.json` 为 root-owned regular file，mode `0600`，link count `1`。
- `endpoint` 精确为 `https://license.yiyong.me/model-api/`。
- model 为测试 mapping 的 default/allowed model。
- 本步骤由真实 HTTPS 协议客户端执行，不等于真实 Electron 或物理 U 盘验收。

## 模型代理

| Method | Path | 条件 | HTTP | code | request ID |
| --- | --- | --- | --- | --- | --- |
| `GET` | `/model-api/v1/models` | 无 bearer | `401` | `AUTHENTICATION_FAILED` | `c154721f-839b-43d9-8c77-a67aa9a479b1` |
| `GET` | `/model-api/v1/models` | 随机 bearer | `401` | `AUTHENTICATION_FAILED` | `5aa3fa0e-ad9c-4b5f-ab9d-7898baa37b43` |
| `GET` | `/model-api/v1/models` | revoked bearer | `401` | `AUTHENTICATION_FAILED` | `e92e6390-34b5-4e41-8b2b-64047973cc76` |
| `GET` | `/model-api/v1/models` | active bearer、允许模型 mapping、不可路由上游 | `502` | `UPSTREAM_UNAVAILABLE` | `36bed7b6-2691-41ba-beae-bb64439c52b4` |
| `GET` | `/model-api/v1/models` | active bearer 重复请求超限 | `429` | `RATE_LIMITED` | `ce056aea-433e-4ff8-8740-a27ad184079c` |
| `POST` | `/model-api/v1/chat/completions` | 无 bearer、合法 JSON body | `401` | `AUTHENTICATION_FAILED` | `cf6dcd9e-580e-4696-b09f-4ec23caffb5f` |
| `POST` | `/model-api/v1/chat/completions` | 随机 bearer、合法 JSON body | `401` | `AUTHENTICATION_FAILED` | `f0b4fb49-0e7f-41b0-ade5-7c638ed27309` |
| `POST` | `/model-api/v1/chat/completions` | revoked bearer、合法 JSON body | `401` | `AUTHENTICATION_FAILED` | `f2bab9ff-b215-4ad5-a209-46ab1da3ee01` |
| `POST` | `/model-api/v1/chat/completions` | active bearer、不允许模型 | `403` | `MODEL_NOT_ALLOWED` | `d470b6f4-e5f6-48ad-9b03-6556618276e9` |

两条路径的 none/random/revoked 六个请求均真实执行，忽略各自 request ID 后，HTTP/code/message 完全一致。`502` 正文仅含公共错误码、标准 message 和 request ID；不含上游 key、device token、内部地址或完整 prompt。不可路由上游和限流只在 `GET /model-api/v1/models` 负向路径验证；`POST /model-api/v1/chat/completions` 验证了鉴权、模型拒绝和双实例接管，不声称 chat 上游或真实模型对话通过。

## 双实例容灾

| 被停实例 | 存活实例 | 存活 loopback health | 公网 health | `POST /model-api/v1/chat/completions` | request ID |
| --- | --- | --- | --- | --- | --- |
| `18081` | `18082` | `200` | `200` | 不允许模型 `403` | `6063b65a-e3cf-4b4d-8688-0f4d036ede84` |
| `18082` | `18081` | `200` | `200` | 不允许模型 `403` | `6bec1962-1a4a-4d48-8cc3-bf6a9b20c40f` |

两轮均只停止一个实例。每轮结束后先恢复被停实例，再进入下一轮。最终双实例和 Caddy 均 active。

## 清理与泄密检查

- 精确 secret scan 覆盖 `/root`、`/etc`、`/opt/uclaw-activation`、`/var/log` 的 932 个 regular files，共 151,738,262 bytes；无命中。
- `caddy`、`uclaw-activation@18081`、`uclaw-activation@18082` journal 无测试 secret；完整测试 prompt 无命中。
- 临时 operator registry 从 1 项原子追加到 2 项；操作结束后恢复原文件 SHA-256 `d1ee0c023c7b1721a60b2d1488361d9e44a7d7960eafd2118a5459f6a13aa6a5`，临时摘要不存在。
- 临时 operator credential、New API key 和 secret pattern 文件已 shred 删除。
- 恢复 registry 后按 `18081`、`18082` 顺序重启，清除进程内临时 operator；两实例 loopback/public health 均为 `200`。

## 未完成门禁

- `license.yiyong.me` DNS、ACME certificate、域名 health。
- GitHub Actions 正式 activation endpoint variable、正式重建、签名和 release feed 回滚证据。
- 独立 New API 安全组/mTLS、真实 key、低余额测试账号和首次模型对话。
- 正常 Desktop 工作区到 `/model-api/v1/chat/completions` 的真实端到端对话。
- 真实 Electron activation-only 客户端、Win10/11 普通用户、Defender、正式发行物和物理 U 盘。
- device-token reissue、admission stale lease 清理和真实 New API key rotation canary 未纳入本轮执行证据。

因此只能声明“VPS 激活与代理负向路径通过；真实模型对话和正式客户端/硬件验收待完成”，不能声明 release-ready。
