# 第一版启动激活授权验收报告

> 历史基线说明：本文保留 2026-08-13 第一轮实现的原始证据，不作为下一次实现契约。下一版改为“用户只输入激活码、长期设备访问令牌、验证服务器模型代理”，以 [激活码、长期设备令牌与模型代理设计](../superpowers/specs/2026-08-13-激活码长期设备令牌与模型代理设计.md) 为准。
>
> 2026-08-14 VPS 重部署与真实协议验收见 [激活码长期令牌模型代理 VPS 验收](./激活码长期令牌模型代理-VPS验收-2026-08-13.md)。该轮已关闭 migration、双实例、IP HTTPS、激活协议和代理负向路径，但域名 DNS/证书、真实 New API 对话、正式 Electron、Windows 和物理 U 盘仍未关闭。

日期：2026-08-13
状态：`blocked` / `not-release-ready`
分支：`codex/activation-deployment-acceptance`

## 验收基线

- 服务端：`codex/go-activation-server@812801774c5c6b8f5a12b465fbe41b6317975c5c`
- 客户端：`codex/desktop-activation-client@07ff88b5d0c4a784f479b1f6e21bedb26fa01468`
- 集成修复：`80ba95d`、`531191d`
- Task 5 记录：`71a6830`

## 证据分层

### 本地合同和单元门禁

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 激活服务 Go | pass | test/race/vet；Linux amd64 ELF 构建成功 |
| Launcher Go | pass | test/race/vet；Windows amd64 PE32+ 交叉编译成功 |
| Node build/typecheck/secret scan | pass | Node.js 24.15.0；各命令退出 0 |
| contract | pass | 24/24 |
| integration | pass | 71 passed、8 skipped |
| 全量 `npm test` | fail | Desktop 1/912 超时并 `ENOTEMPTY`；单独重跑该测试通过。Frontend 1/254 为既有 `#fff` semantic-color baseline。未在本任务修复 |

### Fixture E2E

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| activation flow | pass | `npx playwright test tests/e2e/activation.spec.ts`：10/10 |
| restricted IPC | pass | activation-only 仅五个 IPC；真实 Electron preload fixture 覆盖 |
| 恢复状态 | pass | offline、invalid、write failure、same-disk recovery fixture 覆盖 |
| runtime 数量 | pass | packaging/contract 拒绝第二份 Electron runtime |

Fixture 结果不等于真实服务、Windows 真机或物理介质验收。

### 正式 Windows 和物理 U 盘

| 验收项 | 结果 | 阻塞 |
| --- | --- | --- |
| Win10 x64 普通用户、Defender、无 Node.js | blocked | 无可操作真机和正式发行物 |
| Win11 x64 普通用户、Defender、无 Node.js | blocked | 无可操作真机和正式发行物 |
| 发布目标 U 盘品牌/控制器样本 | blocked | 未提供目标样本清单和实物；本机 `disk4` 未确认属于目标样本 |
| 同盘换电脑、接口、盘符、重启 | blocked | 缺两台 Windows 真机和同一目标物理盘 |
| 双盘同码拒绝 | blocked | 缺两块目标物理盘和正式激活服务 |
| 激活中拔盘与恢复 | blocked | 缺 Windows/物理盘故障注入 |
| 中文/空格路径、普通用户权限 | blocked | 缺正式 Windows 发行物执行证据 |
| Defender 扫描、签名和告警 | blocked | 缺正式签名发行物和 Defender 报告 |
| 正式 release feed/更新/回滚 | blocked | 缺正式签名发行物、CDN 和回滚证据 |

## 安全与隔离结论

- activation-only 路径不启动 OpenClaw Gateway，不暴露工作区或普通 IPC。
- Desktop 首激活与 Launcher gate 共用 startup license 公钥集合。
- activation endpoint 由 Launcher 构建配置注入，继承的 `UCLAW_ACTIVATION_*` 被清理。
- release feed 固定 `updates.u-claw.org`；release/runtime、startup license、status、Token 信任材料保持隔离。
- 仓库证据不含完整激活码、Token、startup secret 或设备指纹。

## 下一版 VPS 部署准备记录

本小节保留 2026-08-14 部署前的只读盘点快照；其中 `blocked` / `pending` 是当时状态，不是后续 VPS 验收结果。操作手册见 [激活与模型代理部署](../runbooks/激活与模型代理部署.md)。

| 准备项 | 状态 | 只读证据或未完成门禁 |
| --- | --- | --- |
| 当前生产拓扑 | confirmed | `postgresql`、`caddy`、`uclaw-activation@18081`、`uclaw-activation@18082` active；Go 仅监听 `127.0.0.1:18081/18082`，Caddy 监听 `80/443` |
| 主部署路径 | confirmed | 宿主 systemd + Caddy + PostgreSQL；compose 仅是新环境参考，不与 VPS 命令混用 |
| 公网 allowlist | blocked | 当前 IP Caddy 基线仅显式拒绝 `/internal/*` 和 `/metrics`；升级时必须改为只允许 `/v1/*`、`/model-api/*`、`/health/ready`，并拒绝管理 API/端口和其他路径 |
| `license.yiyong.me` DNS | blocked | 2026-08-14 `dig +short` 的 A/AAAA 均无输出；部署前 A 必须精确为 `121.41.89.103`、AAAA 空或已单独验证，registry/RDAP 不得有 `clientHold` |
| 域名证书 | blocked | Caddy 尚未完成 `license.yiyong.me` 自动签发/续期验收；取得 SAN、issuer、dates 和 health 证据后才可切端点 |
| IP HTTPS 兼容 | required | 域名切换后至少保留一个完整发行周期；保留已验证 IP TLS 配置和恢复到 IP endpoint 的路径 |
| 公共模型端点 | required | 域名门禁通过后，`PUBLIC_MODEL_ENDPOINT` 与新签发 `builtinCredential.endpoint` 都必须为 `https://license.yiyong.me/model-api/`；同时必须设置 GitHub Actions `vars.UCLAW_ACTIVATION_ENDPOINT=https://license.yiyong.me/`，重建并重新签名/验收正式发行物 |
| New API 网络 | blocked | New API 安全组需只允许源 IP `121.41.89.103`，优先 mTLS，并验证 HTTPS hostname allowlist |
| New API Key 录入/轮换 | blocked | 只允许 root-only `0600` 临时文件；必须先生成全量旧 envelope digest manifest，再 set 新 envelope、canary、分批观察并逐 inventory 证明旧 digest 无引用，最后才在 New API 撤销旧 key；`mapping show` 不得误记为 key fingerprint/list 证据 |
| migration 4 与回滚 | pending | 必须用 migration role 向前应用；旧 binary 回滚不运行旧 migrator，不回滚 schema，不删 migration 4 或新数据 |
| 双实例容灾 | pending | 必须按 18081/18082 滚动，完成 health、kill-one、revoke/reissue、admission stale lease 和 key rotation canary |
| 代理安全 | pending | 必须完成无/随机/revoked bearer 401、不允许模型 403、超限 429、脱敏 502/503 和公网私有路由 404 |

下一版验收证据只记录 git SHA、checksum、migration ledger、证书元数据、HTTP status、request/support ID 和脱敏摘要。禁止写入 activation code、device token、New API Key、startup secret、签名私钥或完整 prompt。

## 2026-08-14 VPS 重部署更新

| 验收项 | 结果 | 脱敏证据 |
| --- | --- | --- |
| 可恢复备份 | pass | `20260814T080046Z` manifest SHA-256 `328f42c8f60c58a272456bbc700a4462aa1f7cb9e6c5b28ba87cf0aa42f59be5`；15/15 entries checksum 通过，含 PostgreSQL custom `activation.dump`/list 2、`uclaw-activation.env` 1、`Caddyfile` 1、systemd units 3、server/admin binaries 4、migration ledger `schema-migrations.txt` 1、metadata/reference 3；`pg_restore --list` 有效（66 entries）；失败目录 `20260814T075910Z` 保留且未复用 |
| migration 4 | pass | ledger 1-4 checksum 全匹配；两个 relation、5 个 constraints、2 个 indexes 存在；app role 的 ledger 写入和 DDL 均以 SQLSTATE `42501` 拒绝 |
| 双实例滚动部署 | pass | release `e84f5ae858018b4775d2307561abcdbdf65ed399`；server/admin SHA-256 分别为 `333deb...5156`、`3745db...727e4`；四服务最终 active |
| live env 与 systemd | pass | `/etc/uclaw-activation.env` SHA-256 `36e272b0119ddd7e7e2b61197f55b6dc728a830bbf1e9f15a8494088e1df20f3`，root:root `0600`、link count 1；`PUBLIC_MODEL_ENDPOINT=https://license.yiyong.me/model-api/`、`NEW_API_ALLOWED_HOSTS=unavailable.invalid`；template SHA-256 `d4e737a096a326b5390b61018d295b41470e150f31cf4a862e8662f84e6e4bfe`，root:root `0644`，`ExecStart` 解析到本 release，实例 `LISTEN_ADDRESS` 分别为 loopback `18081/18082` |
| 公网 allowlist | pass | `/health/ready` 为 `200`；`/metrics`、`/internal/*`、管理/未知路径均为 `404`；app、PostgreSQL、Caddy admin 仅 loopback |
| 激活协议 | pass | 错码 `400`、正确激活 `200`、同幂等恢复 `200`、第二指纹 `409`、commit `204`；凭据文件 mode `0600` |
| 代理安全 | pass | `GET /model-api/v1/models` 实测 none/random/revoked `401`、active allowed upstream `502`、重复请求 `429`；`POST /model-api/v1/chat/completions` 实测 none/random/revoked `401`、active blocked model `403`；两路径六个 auth 失败的归一化正文一致；未声称 chat 上游成功 |
| 双实例容灾 | pass | 分别停止 `18081`、`18082` 时，存活实例 loopback/public health 均为 `200`，`POST /model-api/v1/chat/completions` 不允许模型保持 `403`；随后恢复 active |
| 泄密扫描 | pass | 932 个 regular files、151,738,262 bytes 和 service journal 无测试 secret；完整测试 prompt 无日志命中；临时 operator registry 已恢复 |
| `license.yiyong.me` | `BLOCKED_DOMAIN` | A/AAAA 均为 NXDOMAIN；ACME 明确失败；域名 health 在连接前阻断。新激活仍按合同签发域名模型 endpoint，IP 只作为兼容入口 |
| 真实 New API 对话 | blocked | 仅使用 `unavailable.invalid` 验证脱敏 `502`；未提供独立 New API |
| 正式客户端和硬件 | blocked | 未执行真实 Electron、正式 Windows 发行物、Win10/11、Defender 或物理 U 盘验收 |

## 结论

本地实现、fixture 门禁和 VPS 激活/代理负向路径支持继续正式验收，但发布门禁未关闭。域名 DNS/证书与真实 New API 对话仍 blocked；真实 Electron、Win10/11、普通用户、Defender、正式发行物和物理 U 盘仍无证据。权威状态保持 `not-release-ready`。

恢复条件：提供正式发行物 SHA-256、签名信息、Win10/11 普通用户真机、目标 U 盘样本、Defender 开启证明、正式 HTTPS/DB/KMS 服务与脱敏证据目录，然后从真实首启开始逐项执行。
