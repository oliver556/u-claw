# VPS 部署前只读盘点 - 2026-08-28

## 范围

仅执行只读命令：系统信息、Docker/1Panel 状态、监听端口和既有目录检查。未安装软件，未改服务，未写生产配置。

## 结果

| 节点 | 结果 |
| --- | --- |
| `121.41.89.103` | SSH 可登录；Docker active；PostgreSQL 监听 `127.0.0.1:5432`；Caddy 监听 `80/443`；旧 `uclaw-activation` 双实例监听 `127.0.0.1:18081/18082`；未发现 1Panel/compose。 |
| `64.90.19.251:24851` | SSH 可登录；未安装 Docker/compose/1Panel；仅 SSH 端口监听；公网 `80/443` 当前未开放服务。 |
| `158.51.110.49:14851` | TCP 端口可达，但当前 SSH 密码登录失败；`22` 端口拒绝连接。 |

## 结论

New API / sub2api 的目标源站应为 `158.51.110.49`，但当前无法登录，安装 1Panel 与 Docker 栈需先复核 SSH 凭据。`64.90.19.251` 可先作为前置反代准备；`121.41.89.103` 可继续承载 Bavi-box Cloud API。

## 下一步

1. 复核 `158.51.110.49:14851` 的 root 密码或提供临时 SSH key。
2. 在 `158.51.110.49` 安装 1Panel，创建 PostgreSQL 与 Redis。
3. 用 `u-claw-app-dev/cloud/uclaw-cloud-api/deploy/1panel/` 下的 compose 样板启动 New API / sub2api。
4. 在 `64.90.19.251` 配置 `newapi.yiyong.me`、`sub2api.yiyong.me` 反代和管理路径 allowlist。
