# P3-T08 真实环境执行清单

日期：2026-08-12
基线：`codex/integration-phase4@8d8e69aaaa455fbb77b527cd384b3b840326fe8e`
规则：只接受真实 Windows、物理 U 盘、真实 OpenClaw/服务/账号和真实 UI 操作；证据全脱敏。

- [x] 01 只读确认 HEAD、worktree、脚本、既有报告、主机、磁盘、进程、监听端口和凭据变量名。
- [x] 02 确认当前机为 macOS arm64；无物理 USB、PowerShell、OpenClaw、Windows 发行物或真实验收 JSON。
- [x] 03 确认 Docker daemon 未运行；本机 `127.0.0.1:4173` 为另一工作树 Vite，不能作为真实验收。
- [x] 04 确认 SSH 别名 `rn` host key 已变化；保持 fail-closed，未绕过校验。
- [ ] 05 收齐正式脱敏环境交付单、发行物 SHA-256、服务入口和证据输出目录。
- [ ] 06 Win10 x64 普通用户批次：Defender 开启；物理盘首启、二启、中文空格路径、正常退出、运行中拔盘、宿主残留。
- [ ] 07 Win11 x64 普通用户批次：同盘换机、换盘符、连续性、缓存删除重建、宿主残留；合并两机证据。
- [ ] 08 真实 OpenClaw UI 批次：按主线 A 全部操作，记录 UI 动作、IPC、runtime 请求和真实结果关联证据。
- [ ] 09 四类模型路由批次：国产、自定义、本地、隐藏 builtin；验证真实请求、无回退和秘密隔离。
- [ ] 10 五类渠道批次：Telegram、QQ Bot、飞书、企业微信、个人微信；逐一双向消息和重连。
- [ ] 11 管理面批次：真实 New API、企业上游、授权/制盘服务、TLS、DB、备份、审计和管理员边界。
- [ ] 12 授权与故障批次：制盘、篡改、撤销、reissue、离线、额度、RPM、并发、模型权限、禁用、补偿和幂等。
- [ ] 13 发行批次：runtime 数字签名、更新、失败回滚、缓存重建、卸载；亮暗主题及 100%/125%/150% 缩放。
- [ ] 13A P4 外部能力批次：支持 `tasks.*`/`artifacts.*` 的正式 Gateway、pending/paired Device、在线 Node、正式 Push、OpenAI Realtime 完整对话及 macOS/Electron 权限人工验收。
- [ ] 14 汇总证据；仅真实 Critical/Important 进入修复批次，提交给 90 集成负责人后从失败批次续跑。
- [ ] 15 所有必需矩阵为 `pass` 后才能判定 `release-ready`。

状态只用 `pass/fail/blocked/not-run`。当前恢复点：第 05 项；环境未齐前不得跳到第 06 项。

## 2026-08-13 激活专项快照

- [x] 05A 本地自动化：服务端/Launcher Go 门禁、OpenAPI contract 24/24、integration 71 pass/8 skip、activation E2E 10/10、build/typecheck/secret scan。
- [x] 05A 部署资产静态核验：KMS/secret mount、migration/app role、备份恢复脚本、双实例网关、指标、故障/轮换/恢复 runbook、activation/release trust 隔离。
- [ ] 05B 正式 HTTPS/DB/KMS：库存、人工 New API configured、首次激活、状态查询、24h 缓存。
- [ ] 05B 生命周期：disable/enable/revoke/reissue、旧盘拒绝、新盘激活、Token 刷新资格传播。
- [ ] 05B 故障：响应/commit 丢失、写盘失败、滚动升级、DB failover、备份恢复、双实例 lease holder kill。
- [ ] 06 Win10 x64 普通用户、Defender 开启、无 Node.js、物理 U 盘。
- [ ] 07 Win11 x64 普通用户、同盘换机/接口/盘符/重启、激活中拔盘恢复。

专项状态：`blocked` / `not-release-ready`。恢复点仍为第 05B 项；05A 本地证据不得替代正式环境或 Windows/物理介质证据。
