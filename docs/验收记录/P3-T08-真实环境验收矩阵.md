# P3-T08 真实环境验收矩阵

日期：2026-08-11
当前结论：`not-release-ready`；未发现真实产品缺陷，环境门禁阻塞。

## 本轮真实盘点

| 批次 | 结果 | 证据 |
| --- | --- | --- |
| 基线与工作树 | pass | detached HEAD `333cbae861cc179495efcb17b6a4422921733c2b`；merge-base 相同；P3-T08 工作树盘点前干净 |
| 当前主机 | pass | macOS 26.4.1 build 25E253，arm64；Node 22.18.0/npm 10.9.3；不计 Windows 验收 |
| 物理介质 | blocked | `diskutil list physical` 仅内部 `disk0`；`/Volumes` 仅 Macintosh HD |
| Windows/工具 | blocked | 无 Windows host、`pwsh`/`powershell`、真实 Windows 发行物、真实验收 JSON |
| OpenClaw/UI | blocked | `openclaw` 不存在；无 U-Claw/OpenClaw 进程；另一工作树 Vite `127.0.0.1:4173` 不采信 |
| 服务与账号 | blocked | 未发现真实 New API、授权/制盘、企业上游、渠道账号或生产入口；Docker daemon 未运行 |
| 已知远程入口 | blocked | SSH `rn` host key changed；严格校验拒绝，未连接、未修改 `known_hosts` |

## 主线 A

| 验收项 | 结果 | 精确阻塞 |
| --- | --- | --- |
| Gateway 启停、健康、端口回退 | blocked | 缺真实 Windows 发行物和真实 OpenClaw runtime |
| 会话、历史、发送、流式、停止、重连 | blocked | 缺真实 runtime、模型账号和 UI 操作环境 |
| 工具调用与审批 | blocked | 缺真实工具任务、审批 UI 和 runtime 关联证据 |
| 文件、附件、成果与工作目录 | blocked | 缺物理 U 盘和真实 UI/runtime 文件操作 |
| 记忆 | blocked | 缺真实 OpenClaw 记忆数据及跨机连续性环境 |
| Skill | blocked | 缺真实 Skill 安装、调用、结果和持久化环境 |
| Plugin | blocked | 缺真实 Plugin registry/安装/调用环境 |
| MCP | blocked | 缺真实 MCP server、凭据及工具调用环境 |
| Provider/模型 | blocked | 缺国产、自定义、本地、隐藏 builtin 四类真实端点/模型 |
| Telegram | blocked | 缺脱敏 bot 身份、真实会话和可接收账号 |
| QQ Bot | blocked | 缺脱敏 bot 身份、真实会话和可接收账号 |
| 飞书 | blocked | 缺脱敏应用/租户、真实会话和可接收账号 |
| 企业微信 | blocked | 缺脱敏应用/企业、真实会话和可接收账号 |
| 个人微信 | blocked | 缺真实 Windows 微信登录态和测试联系人 |
| 日志、Doctor、诊断、配置、系统信息 | blocked | 缺真实 Windows/runtime 与 UI 操作 |
| 备份、恢复 | blocked | 缺物理 U 盘真实数据和备份目标 |
| 更新、失败回滚 | blocked | 缺正式签名发行物、HTTPS 更新源及撤销配置 |

## 主线 B

| 验收项 | 结果 | 精确阻塞 |
| --- | --- | --- |
| builtin 隐藏及 builtin/domestic/custom 隔离 | blocked | 缺四类真实端点、请求账号和服务端审计 |
| 启动授权、本地验签、在线查询、撤销、重制、离线 | blocked | 缺正式公钥注入、真实 HTTPS 授权服务、Windows 和物理盘 |
| USB identity、指纹、制盘、New API 开户 | blocked | 缺物理盘、Win10/11、真实制盘服务和 New API |
| 额度、RPM、并发、模型权限、设备/用户禁用 | blocked | 缺真实 New API 管理面、企业上游和多并发客户端 |
| usage、audit、管理员边界 | blocked | 缺真实 DB、审计查询角色和管理员/普通用户账号 |
| 篡改拒绝、撤销传播、补偿、幂等 | blocked | 缺可操作真实授权/制盘服务、故障注入权限和时间窗 |
| 秘密边界 | blocked | 缺真实客户端、服务端、U 盘和宿主机脱敏证据联合审计 |
| runtime 签名、更新、回滚、缓存重建、卸载 | blocked | 缺正式签名五件套、更新源、Win10/11 和物理盘 |

## Windows/USB/UI 矩阵

| 维度 | 结果 | 精确阻塞 |
| --- | --- | --- |
| Win10 x64，普通用户，Defender 开启，无 Node.js | blocked | 未提供可操作真机 |
| Win11 x64，普通用户，Defender 开启，无 Node.js | blocked | 未提供可操作真机 |
| 首启、二启、换盘符、中文空格路径、同盘换机 | blocked | 未提供同一物理 U 盘和两台真机 |
| 运行中拔盘、正常退出、进程清理 | blocked | 未提供物理盘和真机 |
| U 盘唯一持久化、宿主机仅可重建 cache/runtime | blocked | 缺两机 baseline/audit 与真实业务数据 |
| 亮/暗主题，100%/125%/150% | blocked | 缺 Win10/11 各缩放档真实 UI 截图与操作记录 |

## 缺陷与发行

- Critical：0（未执行真实功能批次，不能据此证明无缺陷）。
- Important：0（同上）。
- Minor：0。
- 修复 commit/tests/90：not-run；无真实缺陷，不改产品代码，不移交 90。
- Push/PR：not-run。
