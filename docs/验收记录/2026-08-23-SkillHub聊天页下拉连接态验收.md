# SkillHub 聊天页下拉连接态验收

日期：2026-08-23

## 结论

通过。

聊天页 `SkillHub` 下拉已完成 connected UI 自动验收：入口可见，列表过滤 bundled skills，选择 `browser-automation` 后通过 OpenClaw Agent skills 配置链路保存，并在验收结束后原样还原本机 `openclaw.json`。

## 验收命令

```bash
cd /Users/biancheng/Documents/ChatGPT/U-CLAW/u-claw-app-dev
node scripts/verify-skillhub-chat-dropdown.js
NODE_PATH=/Users/biancheng/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules /Users/biancheng/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/verify-skillhub-connected-ui.js
```

## 结果

```txt
OK SkillHub chat dropdown patch verified
OK SkillHub connected UI acceptance verified
```

## 已验证

1. 聊天输入框工具栏存在 `SkillHub` 下拉。
2. 下拉使用 `skills.status` 加载当前 Agent 可见 skills。
3. 下拉列表不展示 `source === "openclaw-bundled"` 或 `bundled === true` 的 skill。
4. 选择 skill 后合并写入 Agent `skills` allowlist，不覆盖旧 skills。
5. 保存链路复用 `runtimeConfig.patchForm(["agents","list",i,"skills"], ...)` 与 `runtimeConfig.save()`。
6. 验收脚本会备份、验证写入、原样还原本机 `openclaw.json`。

## 剩余风险

1. 第一版只承诺“新会话生效”，不承诺当前 session 立即刷新 tools。
2. `npm start` 内部 Gateway 子进程仍有 `spawn node ENOENT`，本次未获授权修改底座文件。
3. 高风险权限确认、安装事务恢复、ZIP import 仍为 `Blocked`，不得顺带开放。
