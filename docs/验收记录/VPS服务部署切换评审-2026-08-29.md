# VPS 服务部署切换评审

时间：2026-08-29

## 目标拓扑

- `api.yiyong.me`：DNS A 记录指向前置 VPS `64.90.19.251`。
- 前置 VPS：承载公网入口与 New API 反代，转发到本体 VPS `158.51.110.49:3000`。
- 本体 VPS：承载 New API 与 sub2api；New API 仅通过本体 IP/端口被前置访问。
- `sub.yiyong.me`：DNS A 记录指向本体 VPS `158.51.110.49`，直连本体 sub2api。
- 阿里云 VPS `121.41.89.103`：仅保留 Bavi-box license/update/staging 服务，不参与 `api`/`sub` 两条模型服务链路。

## Codex Task 影响

目标 task `codex://threads/01a03a5e-4921-75c2-8f52-d9a0bf77a1ae` 读取不影响对方会话进度。实际影响来自共享 VPS 配置变更。

## 现场状态

### 前置 VPS

```txt
主机：64.90.19.251
实际组件：NGINX + Certbot
1Panel：未发现 1pctl
域名：api.yiyong.me / newapi.yiyong.me
反代目标：http://158.51.110.49:3000
```

已处理：

- 修复 `/etc/nginx/sites-enabled/` 中 `.bak` 配置误启用导致的 duplicate server_name warning。
- 将 `api.yiyong.me` 加入 New API 反代 server block。
- Certbot 已签发覆盖 `newapi.yiyong.me` 与 `api.yiyong.me` 的证书。
- 旧 `sub2api.yiyong.me` upstream 已恢复为本体 VPS `158.51.110.49:8080`，不再指向阿里云。

### 本体 VPS

```txt
主机：158.51.110.49
1Panel：已发现 1pctl，1panel-core / 1panel-agent 运行中
Docker stack：/opt/uclaw-newapi-stack
New API：宿主端口 3000
sub2api：宿主端口 8080
新增入口：Caddy，sub.yiyong.me -> 127.0.0.1:8080
```

已处理：

- 保留原 `uclaw-new-api` 容器，未改 New API 数据。
- 保留原 `uclaw-sub2api` 容器，作为 `sub.yiyong.me` 后端。
- 安装并启用系统 Caddy；Caddy 已为 `sub.yiyong.me` 自动申请证书。

注意：本体域名证书当前由 Caddy 自动续签，不是 1Panel 网站模块。若必须完全由 1Panel 接管，需要在 1Panel 中重新纳管网站与证书。

### 阿里云 VPS

```txt
主机：121.41.89.103
现状：Caddy + Bavi-box activation/update/Cloud API staging + PostgreSQL
域名：license.yiyong.me / updates.yiyong.me
```

已修正：

- 移除误加的 `sub2api.yiyong.me` Caddy host route。
- 停止误部署的阿里云 sub2api Docker stack；未删除留存数据包/数据卷。
- 阿里云不再参与 `api.yiyong.me` 或 `sub.yiyong.me` 链路。

## 公网验证

DNS：

```txt
api.yiyong.me -> 64.90.19.251
sub.yiyong.me -> 158.51.110.49
```

HTTP 验收：

```txt
https://api.yiyong.me/v1/models -> 401 application/json
https://sub.yiyong.me/ -> 200 text/html
https://updates.yiyong.me/healthz -> 200 application/json
```

当前链路：

```txt
api.yiyong.me
-> 前置 VPS NGINX/Certbot
-> 本体 VPS 158.51.110.49:3000
-> uclaw-new-api

sub.yiyong.me
-> 本体 VPS Caddy
-> 127.0.0.1:8080
-> uclaw-sub2api
```

## 结论

当前状态已按用户截图与说明修正：`api` 在前置，反代本体 New API；`sub` 在本体，直连本体 sub2api；阿里云不参与这两条模型服务链路。

仍需确认：

- 前置 VPS 是否确实需要安装/恢复 1Panel；现场未发现 1Panel。
- 本体 `sub.yiyong.me` 是否必须由 1Panel 网站模块管理证书；当前由 Caddy 自动续签。
- 是否保留 `newapi.yiyong.me` / `sub2api.yiyong.me` 旧域名兼容入口。
