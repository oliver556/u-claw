# U-Claw 第一阶段阶段性验收记录

> 验收日期：2026-08-08  
> 验收范围：当前主工作区 `codex/client-product-foundation`  
> 结论：本文件保留 Task 9 前历史快照；第一阶段整体未通过  
> 最新 Task 口径：`docs/第一阶段任务对齐表.md`

## 0. 2026-08-09 对齐补记

四个集成提交已 fast-forward 合入长期目标分支。按最新功能矩阵：44 项基础及第一阶段需求中，16 项已完成、20 项开发中、4 项待验收、4 项未开始、0 项审计中。

阶段进度不再使用零散功能数或历史批次编号，统一使用 23 个 `P1-Txx`：6 个已完成、13 个开发中、2 个待验收、0 个审计中、2 个未开始，严格完成度 `6/23 = 26.1%`。

### 0.1 本次集成证据与严格结论

- 集成范围：`ecaca48`、`3879257`、`6c4d5c9`、`2c7d9ef`。
- 窗口 90 验证：精确 Node/npm 环境下 `npm ci`、build、typecheck、582 项测试、integration `10/10`、两个 Go module test/vet、Windows amd64 交叉构建、Windows static contract `79/79`。
- `P1-T04`/`SEC-002` 自动化验收全绿，满足文档完整验收标准，标记已完成。
- `P1-T01` 代码、精确 Node/npm、干净安装和完整本地门禁通过，标记已完成；本次未 push，GitHub Actions matrix 未触发但不再阻塞该 Task。
- `P1-T05` 非 100%：代码与自动化完成；真实双机、同一物理 U 盘、换盘符、PowerShell 5.1/7、真实配置/会话/记忆/文件连续性、host residue audit 和物理拔盘证据缺失，标记待验收。
- `P1-T06` 非 100%：代码与自动化完成；真实微信插件发布 artifact 审计、真实 Windows、物理 U 盘、真实个人微信登录前后残留证据缺失，标记待验收。
- Windows 交叉构建、static contract 和 mock 均不代表 Windows 实机通过。

## 1. 已通过需求

- `MODEL-007` 模型能力与版本检测
- `SEC-001` Renderer/IPC 安全边界
- `SEC-002` 日志与错误脱敏
- `REL-002` 锁定 OpenClaw 版本
- `RUN-007` Gateway 启动与端口回退
- `RUN-008` Gateway 健康检查
- `CHAT-001` 会话列表与切换
- `CHAT-005` 消息发送与流式输出
- `CHAT-006` 停止生成
- `CHAT-007` Markdown 安全渲染
- `CHAT-010` 断线重连与草稿保留
- `OPS-008` 高级控制台
- `UX-001` “随行工作舱”应用骨架
- `UX-003` 原生一级导航
- `UX-004` 响应式工作区
- `UX-007` Ant Design 蓝白亮色主题

## 2. 验证证据

执行环境：macOS，Node.js `v22.18.0`，npm `10.9.3`。当前 Node.js 版本不是目标 runtime `v24.15.0`，因此不作为 Windows runtime 验收证据。

执行命令与结果：

```text
cd product && npm test
通过：脚本测试 19、契约测试 11、shared 121、adapter 73、desktop 78、frontend 56；共 358 项。

cd product && npm run test:integration
通过：10 项。

cd product && npm run test:e2e
通过：20 项。

cd product && npm run typecheck
通过。

cd product && npm run build
通过；dist smoke 通过。Vite 报告单个 854.77 kB chunk 警告。
```

## 3. 待验收与部分完成需求

待验收：`DATA-003`、`DATA-004`、`DATA-005`、`DATA-006`，分别归属非 100% 的 `P1-T05`、`P1-T06`。

部分完成：`RUN-001`、`RUN-002`、`RUN-004`、`RUN-009`、`RUN-011`、`CHAT-002`、`CHAT-004`、`CHAT-009`、`CHAT-011`、`CHAT-012`、`OPS-006`、`UX-002`、`UX-006`。

待验收需求已有代码和当前自动化证据，但尚未满足各自完整 CI 或真实环境验收标准，不勾选。

## 4. 未达到验收需求

`RUN-003`、`RUN-005`、`RUN-006`、`RUN-010`、`RUN-012`、`RUN-013`、`CHAT-008`、`OPS-010`、`REL-004`、`SEC-003`、`UX-005`。

## 5. 阶段阻断

- 新增产品 GitHub Actions Windows 2022 matrix 尚未实际触发。
- 尚无 Windows 10/11 实机验收记录。
- 尚无同一物理 U 盘的双机、换盘符、PowerShell 5.1/7、真实数据连续性、拔盘和宿主机残留审计证据。
- 尚无真实 `@tencent-weixin/openclaw-weixin` 发布 artifact 审计和真实个人微信登录前后残留证据。
- 附件选择发送、会话重命名删除、模型选择、完整上下文舱等功能仍有缺口。

第一阶段只有在 44 项清单全部勾选且无阻断项后，才能宣布完成。
