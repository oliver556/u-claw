# U-Claw 第四阶段联调验收矩阵

> 记录日期：2026-08-11
> 状态基线：`codex/integration-p2-t10-local@9bcd854`
> 规则：只记真实后端和权威文件证据，不记 Mock/fixture 成功为接通。

## 1. 状态定义

| 状态 | 定义 |
| --- | --- |
| `未接线` | UI 仍是 Mock、固定数据、空数组、`Unsupported`，或尚无 UI |
| `已接线待联调` | 生产调用链存在，但没有真实成功证据 |
| `本地真实通过` | 真实本地 OpenClaw/Gateway/文件/本地服务成功和失败路径通过 |
| `待外部环境` | 仅缺真实账号、Windows、物理 U 盘或远程服务 |
| `已验收` | 指定正式环境的成功、失败、重启和读回证据完整 |
| `不迁移` | 已批准的内部能力，附明确理由和替代路径 |

每行完成时补充：环境、实际请求/事件、成功证据、失败证据、代码提交和验收人。

## 2. `P4-T01` 生产装配

| ID | UI/用户结果 | 权威后端 | 当前诊断 | 状态 |
| --- | --- | --- | --- | --- |
| `WIR-001` | 正式 Electron 启动新 UI | Electron entry | 仓内 factory 启动真实 Electron、preload 和生产 renderer | `本地真实通过` |
| `WIR-002` | Gateway 启动与端口回退 | OpenClaw Gateway process | 生产 factory 启动锁定 runtime，健康后创建窗口并受控清理 | `本地真实通过` |
| `WIR-003` | WebSocket v4 连接与鉴权 | Gateway WebSocket | 实机 v4 challenge/token auth 与 backend client shape 通过 | `本地真实通过` |
| `WIR-004` | 能力协商与不支持状态 | `hello-ok.features` | 实机 `hello-ok`、方法协商和 `sessions.list` 通过，缺方法显式 `UNSUPPORTED` | `本地真实通过` |
| `WIR-005` | 附件选择和受控导入 | Electron dialog/Attachment Service | 同一生产 client graph 组装 dialog、受控读取与 AttachmentManager | `本地真实通过` |
| `WIR-006` | 当前 U 盘、Gateway、数据落盘状态 | Launcher/Desktop/Gateway | 生产状态投影来自 Gateway process 与 portable data root | `本地真实通过` |
| `WIR-007` | 无配置时显示可诊断错误 | Desktop startup | `UNCONFIGURED/OFFLINE/AUTH_FAILED/PROTOCOL_ERROR/UNSUPPORTED` 分型并显示安全错误框 | `本地真实通过` |
| `WIR-008` | 正式模式不回退 Mock | Electron/React bootstrap | 正式源码无 Mock fallback；无 preload 仅显示明确开发预览 | `本地真实通过` |

本地实证（2026-08-11，macOS，Node `24.15.0`，OpenClaw `2026.7.1-2`）：

- 成功：生产 factory 真实 spawn Gateway；`GET /ready`、WebSocket v4 token 鉴权、`hello-ok`、`sessions.list` 和 SIGINT cleanup 均成功，进程 exit `0`。
- 成功：真实 Electron 加载 `desktop/dist/entry.js`、真实 preload 和 `frontend/dist`；title=`U-Claw`、preload bridge=`function`、`developmentPreview=false`、`gateway.negotiate` protocol=`4`、`sessions.list` 成功，exit `0`。
- 失败：空生产环境由 factory 返回 `UNCONFIGURED`；不安全的 macOS symlink cache root 被 portable 边界拒绝并安全映射为启动 `OFFLINE`，错误输出和 dialog 不含 token、路径或原始异常。
- 代码与验证：本次 P4-T01 原子提交；shared/adapter/desktop/frontend typecheck、unit、contract、integration、renderer E2E、真实 Gateway/Electron smoke 与 `git diff --check` 通过。

## 3. `P4-T02` 工作、会话、文件与记忆

| ID | UI/用户结果 | 权威后端 | 当前诊断 | 状态 |
| --- | --- | --- | --- | --- |
| `WORK-001` | 会话列表和历史分页 | `sessions.list`、`chat.history` | Adapter 已写，只见 Mock | `未接线` |
| `WORK-002` | 新建、切换、重命名、删除会话 | `sessions.create/describe/patch/delete` | Adapter 已写，缺生产链 | `未接线` |
| `WORK-003` | 刷新/重启后会话不丢 | OpenClaw session store | 浏览器 Mock 仅内存 | `未接线` |
| `WORK-004` | 会话搜索、固定和分组 | OpenClaw + `session-organizer.json` | 浏览器缺 organizer，操作无效 | `未接线` |
| `WORK-005` | 发送、流式输出、停止 | `chat.send/abort` + events | Adapter 已写，只见 Mock | `未接线` |
| `WORK-006` | 附件选择、拖放、发送和重试 | OpenClaw attachment payload | 缺真实 Gateway 证据 | `未接线` |
| `WORK-007` | 工具调用轨迹和参数/结果摘要 | `session.tool`、`tools.catalog/get` | Adapter 已写，缺生产链 | `未接线` |
| `WORK-008` | Exec/Plugin 单次审批 | approval RPC/events | Adapter 已写，缺生产链 | `未接线` |
| `WORK-009` | 上下文附件、引用、记忆、工具结果 | Session/Message/Files | 部分从消息推导 | `未接线` |
| `WORK-010` | 工作区文件列表、搜索、读取 | U 盘 `workspace` | 浏览器固定空数组 | `未接线` |
| `WORK-011` | 文件打开、显示位置、重命名、移动、删除 | Desktop Data Service | 有服务，缺生产 bridge | `未接线` |
| `WORK-012` | Markdown 记忆列表、搜索、读取 | `MEMORY.md`、`memory/*.md` | 浏览器固定空数组 | `未接线` |
| `WORK-013` | 记忆编辑、删除与 OpenClaw 读回 | U 盘记忆目录/OpenClaw | 有文件服务，缺 OpenClaw 一致性证据 | `未接线` |
| `WORK-014` | Session 文件列表和读取 | `sessions.files.*` | 无 Adapter/UI 直接映射 | `未接线` |
| `WORK-015` | reset/compact/branch/restore/steer | Sessions 高级 RPC | 无 UI/契约 | `未接线` |

## 4. `P4-T03` 模型、Provider、配置与用量

| ID | UI/用户结果 | 权威后端 | 当前诊断 | 状态 |
| --- | --- | --- | --- | --- |
| `MODEL-001` | Provider 列表、新增、编辑、启停、排序 | OpenClaw config + Provider Store | Store 与 OpenClaw 配置未同步 | `未接线` |
| `MODEL-002` | API Key 写入、轮换、脱敏 | 受控凭据存储/OpenClaw config | UI/Store 已有，未证明实际请求使用 | `未接线` |
| `MODEL-003` | OpenAI-compatible 自定义对话 | custom executor | 生产 executor 未提供 | `未接线` |
| `MODEL-004` | 国产 Provider 实际对话 | domestic executor | 生产 executor 未提供 | `未接线` |
| `MODEL-005` | Ollama/LM Studio 发现 | 本机 API | 有真实探测服务，缺 Electron 联调 | `未接线` |
| `MODEL-006` | 模型连通测试 | Provider endpoint | 有真实 HTTP 服务，缺账号/生产链 | `未接线` |
| `MODEL-007` | Proxy/`NO_PROXY` 保存与探测 | Desktop network | 有服务，未证明 Gateway/executor 同步 | `未接线` |
| `MODEL-008` | 会话选模并写后读回 | `models.list`、`sessions.patch` | Adapter 已写，缺生产链 | `未接线` |
| `MODEL-009` | builtin/domestic/custom 三类路由 | Model Router/New API/Provider | 有路由与 fixture，缺正式 executor | `未接线` |
| `MODEL-010` | OpenClaw 原始配置 schema-driven 管理 | `config.get/schema/patch/apply` | 只有脱敏只读视图和自有 Store | `未接线` |
| `MODEL-011` | OpenClaw/New API 用量、成本、时序 | `usage.*`、`sessions.usage*` | 无 UI/契约 | `未接线` |

## 5. `P4-T04` Skill、Plugin、MCP、工具与审批

| ID | UI/用户结果 | 权威后端 | 当前诊断 | 状态 |
| --- | --- | --- | --- | --- |
| `CAP-001` | 原内置中文 Skill 列表 | `portable/skills-cn`/runtime | 新 Skill Service 未扫描原目录 | `未接线` |
| `CAP-002` | OpenClaw 已安装 Skill 状态 | `skills.status` | Adapter 显式 `Unsupported` | `未接线` |
| `CAP-003` | SkillHub 搜索、分类、详情 | SkillHub.cn | live client 已有，缺页面真实联调 | `未接线` |
| `CAP-004` | Skill 安装、更新、卸载 | SkillHub + U 盘 + OpenClaw | 自有目录操作已有，缺 OpenClaw 读回 | `未接线` |
| `CAP-005` | Skill 启用、禁用、依赖和冲突 | OpenClaw Skill runtime | 未证明实际 Agent 调用生效 | `未接线` |
| `CAP-006` | Skill curator/proposals | `skills.curator/proposals.*` | 无 UI/契约 | `未接线` |
| `CAP-007` | Plugin 列表和安装生命周期 | OpenClaw CLI/registry | 有 Service，缺 registry/wiring/读回 | `未接线` |
| `CAP-008` | Plugin UI descriptors/session actions | Plugin runtime RPC | 无真实映射 | `未接线` |
| `CAP-009` | MCP 配置增删改启停 | `config.get/patch` | runtime adapter 已有，缺生产链 | `未接线` |
| `CAP-010` | MCP 工具投影 | `tools.effective` | 未接权威投影 | `未接线` |
| `CAP-011` | MCP 实时 probe | 受控 protocol/CLI probe | probe 实现已有，缺真实 server | `未接线` |
| `CAP-012` | 工具 catalog/effective 管理视图 | `commands.list`、`tools.catalog/effective` | 只有会话工具轨迹 | `未接线` |
| `CAP-013` | Exec/Plugin 审批策略 | `exec.approvals.get/set` | 只有单次审批 | `未接线` |
| `CAP-014` | 能力风险提示与实际权限一致 | OpenClaw 有效工具/策略 | UI 有提示，未证明真实执行策略 | `未接线` |

## 6. `P4-T05` 全渠道

| ID | UI/用户结果 | 权威后端 | 当前诊断 | 状态 |
| --- | --- | --- | --- | --- |
| `CONN-001` | Telegram 配置与脱敏凭据 | OpenClaw config | Adapter 已写，缺生产链 | `未接线` |
| `CONN-002` | Telegram 启停、测试、重连、收发 | Telegram runtime | 仅 fixture 证据 | `未接线` |
| `CONN-003` | QQ Bot 配置和白名单 | OpenClaw QQ Plugin/CLI | 只有 UI/Store | `未接线` |
| `CONN-004` | QQ Bot 启停、测试、重连、收发 | QQ runtime | 无 Adapter runtime | `未接线` |
| `CONN-005` | 飞书配置、启停、回调、收发 | Feishu runtime | 只有 UI/Store | `未接线` |
| `CONN-006` | 企业微信配置、启停、回调、收发 | WeCom runtime | 只有 UI/Store | `未接线` |
| `CONN-007` | 个人微信插件状态 | OpenClaw WeChat Plugin | capability 可查，操作不可用 | `未接线` |
| `CONN-008` | 个人微信扫码、刷新、超时、取消 | 已有 WeChat HTTP/Plugin flow | 新 Adapter 显式 `Unsupported` | `未接线` |
| `CONN-009` | 个人微信登录、重连、登出 | WeChat runtime | 新 Adapter 显式 `Unsupported` | `未接线` |
| `CONN-010` | Discord 配置 | OpenClaw Discord channel | 无 UI/契约 | `未接线` |
| `CONN-011` | Discord 启停、测试、重连、收发 | Discord runtime | 无 UI/Adapter | `未接线` |
| `CONN-012` | 统一渠道健康和凭据状态 | `channels.status` | 当前 Store 投影不是全 runtime | `未接线` |
| `CONN-013` | 渠道 logout/send/action/poll | OpenClaw channel RPC | 除 Telegram 启停外未映射 | `未接线` |

## 7. `P4-T06` Agent、Cron、Task 与成果

| ID | UI/用户结果 | 权威后端 | 当前诊断 | 状态 |
| --- | --- | --- | --- | --- |
| `AUTO-001` | Agent 列表和身份 | `agents.list`、`agent.identity.get` | 无 UI/契约 | `未接线` |
| `AUTO-002` | Agent 创建、编辑、删除 | `agents.create/update/delete` | 无 UI/契约 | `未接线` |
| `AUTO-003` | Agent 文件列表、读取、写入 | `agents.files.*` | 无 UI/契约 | `未接线` |
| `AUTO-004` | Agent workspace 列表与读取 | `agents.workspace.*` | 当前文件页只读本地工作区 | `未接线` |
| `AUTO-005` | Cron 列表、状态和详情 | `cron.list/status/get` | 无 UI/契约 | `未接线` |
| `AUTO-006` | Cron 新增、更新、删除 | `cron.add/update/remove` | 无 UI/契约 | `未接线` |
| `AUTO-007` | Cron 立即运行和运行历史 | `cron.run/runs` | 无 UI/契约 | `未接线` |
| `AUTO-008` | 真实后台 Task 列表 | `tasks.list` | 当前从 sessions/chat 派生 | `未接线` |
| `AUTO-009` | Task 详情 | `tasks.get` | 无权威映射 | `未接线` |
| `AUTO-010` | Task 取消 | `tasks.cancel` | 无 UI/契约 | `未接线` |
| `AUTO-011` | Task 实时事件 | `task` event | 当前轮询派生快照 | `未接线` |
| `AUTO-012` | Artifact 权威列表和详情 | `artifacts.list/get` | 当前从 chat message 推导 | `未接线` |
| `AUTO-013` | Artifact 下载/打开 | `artifacts.download` + U 盘 | 无权威 download 映射 | `未接线` |
| `AUTO-014` | 成果来源会话回溯 | Artifact/Session | 只在派生数据中存在 | `未接线` |

## 8. `P4-T07` 高级系统与产品化

| ID | UI/用户结果 | 权威后端 | 当前诊断 | 状态 |
| --- | --- | --- | --- | --- |
| `SYS-001` | OpenClaw/Desktop/Launcher 日志列表与追加 | `logs.tail` + owned logs | OpenClaw listLogs `Unsupported` | `未接线` |
| `SYS-002` | 日志筛选、导出和清理 | Desktop Diagnostics | 有服务，缺生产数据 | `未接线` |
| `SYS-003` | health/status/system.info | Gateway/OS | 局部投影，缺真实运行 | `未接线` |
| `SYS-004` | diagnostics.stability/audit | Gateway diagnostics/audit | 无完整 UI/契约 | `未接线` |
| `SYS-005` | Doctor 检查和受控修复 | Gateway/CLI Doctor | 只有部分 Doctor 和 restart | `未接线` |
| `SYS-006` | 备份、恢复、保留和回滚 | U 盘 + consistency coordinator | 有服务，缺生产 wiring | `未接线` |
| `SYS-007` | 存储统计、清理和恢复出厂 | U 盘 + Desktop maintenance | 有服务，缺生产 wiring | `未接线` |
| `SYS-008` | 原始配置脱敏读取/导出 | `config.get` | 有安全视图，缺真实 config | `未接线` |
| `SYS-009` | 设备配对列表、批准、拒绝、移除 | `device.pair.*` | 无 UI/契约 | `未接线` |
| `SYS-010` | 设备 Token 轮换和撤销 | `device.token.*` | 无 UI/契约 | `未接线` |
| `SYS-011` | Node 列表、详情、重命名 | `node.list/describe/rename` | 无 UI/契约 | `未接线` |
| `SYS-012` | Node 配对和移除 | `node.pair.*` | 无 UI/契约 | `未接线` |
| `SYS-013` | Node 受控 invoke 和事件 | `node.invoke/event` | 无 UI/契约 | `未接线` |
| `SYS-014` | Environment 列表与状态 | `environments.list/status` | 无 UI/契约 | `未接线` |
| `SYS-015` | Worktree 列表、创建、删除、恢复、GC | `worktrees.*` | 无 UI/契约 | `未接线` |
| `SYS-016` | 受控原生 Terminal | `terminal.*` | 无原生 UI，只有 Dashboard 兜底 | `未接线` |
| `SYS-017` | Talk 会话 | `talk.session/client.*` | 无 UI/契约 | `未接线` |
| `SYS-018` | TTS 状态、Provider、Persona 和说话 | `tts.*` | 无 UI/契约 | `未接线` |
| `SYS-019` | Voice Wake 和路由 | `voicewake.*` | 无 UI/契约 | `未接线` |
| `SYS-020` | Push 订阅、取消和测试 | `push.*` | 无 UI/契约 | `未接线` |
| `SYS-021` | 安全更新、签名、切换和回滚 | Release feed/Desktop/Launcher | 有服务，缺正式 feed/公钥 | `未接线` |
| `SYS-022` | 启动授权、撤销、重制和离线容错 | Launcher/License Service | 本地自动化已有，缺真实服务 | `未接线` |
| `SYS-023` | USB 身份、制盘、New API 开户和补偿 | Launcher/Provisioning/New API | localhost 自动化已有，缺真实服务 | `未接线` |
| `SYS-024` | 额度、限速、并发、模型权限、禁用和用量 | New API management/data plane | localhost 自动化已有，缺真实服务 | `未接线` |

## 9. `P4-T00` 不迁移决策

| ID | 能力 | 结论 | 理由 | 状态 |
| --- | --- | --- | --- | --- |
| `AUD-001` | `attach.grant/revoke` | Adapter 内部使用 | 连接权限内部控制，不直接暴露用户 | `不迁移` |
| `AUD-002` | `secrets.reload/resolve` | Electron/服务端内部使用 | 避免 renderer 获取秘密 | `不迁移` |
| `AUD-003` | `controlUi.githubPreview` | 不迁移 | Dashboard 内部预览 | `不迁移` |
| `AUD-004` | `crestodian.*` | 不迁移 | 无已批准用户需求 | `不迁移` |

## 10. 当前统计

| 类型 | 数量 |
| --- | ---: |
| 用户功能行 | 99 |
| `未接线` | 91 |
| `已接线待联调` | 0 |
| `本地真实通过` | 8 |
| `待外部环境` | 0 |
| `已验收` | 0 |
| 已批准 `不迁移` 内部行 | 4 |

用户功能行完成度：`8 / 99 = 8.1%`。其余功能仍按真实生产后端证据逐项推进。
