{
  "name": "@uclaw/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@uclaw/adapter": "0.1.0",
    "@uclaw/shared": "0.1.0"
  },
  "devDependencies": {
    "electron": "^40.4.0",
    "vitest": "^3.2.4"
  }
}
